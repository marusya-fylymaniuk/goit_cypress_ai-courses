# 🚀 План оптимізації тестів

## Що я зробив зараз:

### ✅ Крок 1: Створена структура папок
```
cypress/e2e/
├── pages/              # НОВА папка - тести по сторінках
│   └── _helpers.js    # Helper функції
└── suites/            # НОВА папка - для зворотної сумісності (опціонально)
```

### ✅ Крок 2: Створений helper файл
`cypress/e2e/pages/_helpers.js` - містить спільні функції:
- `TEST_LEAD` - тестові дані
- `normalizeUrlForCanonical()` - нормалізація URL
- `safeBodyText()` - безпечне читання body
- `getModalHelpers()` - функції для роботи з модалкою

---

## 📋 Що потрібно зробити далі:

### Крок 3: Створити файли для кожної сторінки

Для кожної з 8 сторінок потрібно створити файл `cypress/e2e/pages/{page-key}.cy.js`

**Структура файлу:**
```javascript
/// <reference types="cypress" />
const pages = require('../../fixtures/pages.json');
const { normalizeUrlForCanonical, getModalHelpers, TEST_LEAD } = require('./_helpers');

// Знайти сторінку за ключем
const pageKey = Cypress.env('PAGE_KEY') || 'ai-marketing-specialist';
const page = pages.find(p => p.key === pageKey);

describe(`${page.name} - All Tests`, () => {
  beforeEach(() => {
    cy.viewport(1280, 720);
    cy.visit(page.url);
    cy.window().then((win) => win.scrollTo(0, 0));
  });

  // ============================================
  // TC#1: Smoke / Sanity / SEO
  // ============================================
  describe('Smoke / Sanity / SEO checks', () => {
    // Всі тести з 01-smoke-sanity.cy.js
  });

  // ============================================
  // TC#2: Header Navigation
  // ============================================
  describe('Header / Menu / Navigation', () => {
    // Всі тести з 02-header-navigation.cy.js
  });

  // ============================================
  // TC#3: Language Switcher
  // ============================================
  describe('Language switcher / Localization', () => {
    // Всі тести з 03-language-switcher.cy.js
  });

  // ... і так далі для всіх 16 тест-кейсів
});
```

---

## 🔧 Як об'єднати тести:

### Варіант 1: Вручну (повний контроль)

1. Відкрити кожен з 16 тест-файлів
2. Знайти тести для конкретної сторінки (вони в `targetPages.forEach`)
3. Скопіювати тести в новий файл сторінки
4. Повторити для всіх 8 сторінок

**Час:** ~2-3 години
**Переваги:** Повний контроль, можна оптимізувати
**Недоліки:** Багато ручної роботи

---

### Варіант 2: Скрипт для автоматизації (рекомендовано)

Створити Node.js скрипт, який:
1. Читає всі тест-файли
2. Витягує тести для кожної сторінки
3. Об'єднує їх в нові файли

**Час:** ~30 хвилин (написання скрипту) + ~5 хвилин (виконання)
**Переваги:** Швидко, автоматично
**Недоліки:** Потрібно перевірити результат

---

### Варіант 3: Поступово (безпечно)

1. Створити один файл як приклад (ai-marketing-specialist.cy.js)
2. Протестувати його
3. Якщо працює - створити інші за аналогією

**Час:** ~1 година на файл × 8 = ~8 годин
**Переваги:** Безпечно, можна тестувати поступово
**Недоліки:** Довго

---

## 💡 Моя рекомендація:

**Створити скрипт для автоматизації (Варіант 2)**

Скрипт буде:
1. Читати `pages.json` → отримує список сторінок
2. Для кожної сторінки:
   - Читати всі 16 тест-файлів
   - Витягувати тести для цієї сторінки
   - Об'єднувати в один файл
3. Створити 8 нових файлів

**Чи створити цей скрипт зараз?**

---

## 📝 Після створення файлів:

### Крок 4: Оновити package.json

Додати нові скрипти:
```json
{
  "scripts": {
    "test:pages": "cypress run --browser chrome --spec 'cypress/e2e/pages/**/*.cy.js'",
    "test:page": "cypress run --browser chrome --spec 'cypress/e2e/pages/{page-key}.cy.js'",
    // ... інші скрипти
  }
}
```

### Крок 5: Тестування

1. Запустити нові тести
2. Перевірити, що всі тести проходять
3. Порівняти час виконання

### Крок 6: Оновити документацію

Оновити README.md з новими скриптами

---

## ⚠️ Важливо:

- **Стару структуру не видаляти одразу** - залишити для зворотної сумісності
- **Тестувати поступово** - спочатку один файл, потім всі
- **Зберегти можливість запускати окремі тест-кейси** (через suites/)

---

## 🎯 Очікуваний результат:

**До оптимізації:**
- 128 відкриттів сторінок (8 сторінок × 16 тест-файлів)
- ~29 хвилин виконання

**Після оптимізації:**
- 8 відкриттів сторінок (по одному на сторінку)
- ~22-23 хвилини виконання
- **Економія: ~6-7 хвилин (20-25%)**

---

**Готовий продовжити? Якщо так, створю скрипт для автоматизації або почну створювати файли вручну?**



