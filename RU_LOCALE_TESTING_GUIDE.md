# 🇷🇺 Гайд: Тестування російської локалі (ua-ru) по всім тест-кейсам

## 📍 Куди я додав нові перевірки

**Примітка:** Спочатку я додав тест відправки форми на ру локалі в тест-кейс #3, але потім його прибрали, оскільки відправка форми - це частина тест-кейсу #5 (Modal form), який автоматично працює для обох локалей через `pages.json`.
**Файл:** `cypress/e2e/03-language-switcher.cy.js`  
**Тест:** `form submission works on Russian locale (ua-ru)`

**Що перевіряє:**
- Чи відкривається модальна форма на російській локалі
- Чи можна заповнити форму
- Чи відправляється форма успішно
- Чи з'являється повідомлення про успіх (thank-you)

**Як працює:**
1. Переходить на російську локаль (`/ua-ru/`)
2. Відкриває модальну форму з Hero секції
3. Заповнює форму тестовими даними:
   - Ім'я: `testmarusyacypress`
   - Телефон: `+380635080808`
   - Email: `testmarusyacypress2026@qa.team`
4. Відправляє форму (stub - без створення реального лідa)
5. Перевіряє, що форма відправлена успішно

**Важливо:** Цей тест використовує **stub** (заглушку), тому реальний лід НЕ створюється. Це безпечно для тестування.

---

## 🎯 Як тестувати ру локаль по всім тест-кейсам

### **Окреме тестування локалей (рекомендовано) ✅**

Тепер можна **тестувати українську та російську локалі окремо**!

**Що я зробив:**
1. Додав **російські локалі в `pages.json`**:
   - `ai-marketing-specialist-ru` → `https://goit.global/ua-ru/courses/ai-marketing-specialist/`
   - `ai-content-maker-ru` → `https://goit.global/ua-ru/courses/ai-content-maker/`

2. Додав **фільтрацію по локалі** в усі тести через env змінну `LOCALE`

3. Додав **npm scripts** для окремого запуску кожної локалі

---

## 🚀 Швидкий старт

### **Тестувати тільки українську локаль (ua):**

```bash
# Всі тести для укр локалі
npm run test:ua

# Окремі тест-кейси для укр локалі
npm run test:smoke:ua
npm run test:header:ua
npm run test:language:ua
npm run test:hero:ua
npm run test:modal:ua
```

### **Тестувати тільки російську локаль (ua-ru):**

```bash
# Всі тести для ру локалі
npm run test:ru

# Окремі тест-кейси для ру локалі
npm run test:smoke:ru
npm run test:header:ru
npm run test:language:ru
npm run test:hero:ru
npm run test:modal:ru
```

### **Тестувати обидві локалі (за замовчуванням):**

```bash
# Всі тести для обох локалей
npm run cy:run

# Окремі тест-кейси для обох локалей
npm run test:smoke
npm run test:header
npm run test:language
npm run test:hero
npm run test:modal
```

---

## 📋 Доступні npm scripts

### **Загальні (обидві локалі):**
- `npm run cy:run` - всі тести для обох локалей
- `npm run test:smoke` - smoke тести для обох локалей
- `npm run test:header` - header тести для обох локалей
- `npm run test:language` - language switcher тести для обох локалей
- `npm run test:hero` - hero тести для обох локалей
- `npm run test:modal` - modal тести для обох локалей

### **Тільки українська локаль (ua):**
- `npm run test:ua` - всі тести для укр локалі
- `npm run test:smoke:ua` - smoke тести для укр локалі
- `npm run test:header:ua` - header тести для укр локалі
- `npm run test:language:ua` - language switcher тести для укр локалі
- `npm run test:hero:ua` - hero тести для укр локалі
- `npm run test:modal:ua` - modal тести для укр локалі

### **Тільки російська локаль (ua-ru):**
- `npm run test:ru` - всі тести для ру локалі
- `npm run test:smoke:ru` - smoke тести для ру локалі
- `npm run test:header:ru` - header тести для ру локалі
- `npm run test:language:ru` - language switcher тести для ру локалі
- `npm run test:hero:ru` - hero тести для ру локалі
- `npm run test:modal:ru` - modal тести для ру локалі

---

## 🔧 Як це працює технічно

**Фільтрація по локалі:**
Всі тести тепер підтримують env змінну `LOCALE`:

```javascript
const localeFilter = Cypress.env('LOCALE'); // 'ua' або 'ua-ru'

// Фільтруємо сторінки по локалі
let filteredPages = pages;
if (localeFilter) {
  filteredPages = pages.filter((p) => p.locale === localeFilter);
}
```

**Приклад використання:**
```bash
# Тестувати тільки укр локаль
cypress run --env LOCALE=ua

# Тестувати тільки ру локаль
cypress run --env LOCALE=ua-ru

# Тестувати обидві локалі (за замовчуванням)
cypress run
```

---

## 📊 Структура pages.json після змін

```json
[
  // Українська локаль
  {
    "key": "ai-marketing-specialist",
    "url": "https://goit.global/ua/courses/ai-marketing-specialist/",
    "locale": "ua"
  },
  
  // Російська локаль (НОВЕ!)
  {
    "key": "ai-marketing-specialist-ru",
    "url": "https://goit.global/ua-ru/courses/ai-marketing-specialist/",
    "locale": "ua-ru"
  },
  
  // ... і так далі для інших курсів
]
```

**Важливо:** Кожен курс тепер має **два записи** - для української та російської локалей.

---

## 🔍 Які тест-кейси тепер працюють для ру локалі

### **Тест-кейс #1: Smoke / Sanity ✅**
- Перевіряє базові речі (завантаження, H1, мета-теги)
- **Автоматично працює** для ру локалі

### **Тест-кейс #2: Header / Menu / Navigation ✅**
- Перевіряє навігацію, меню, dropdown
- **Автоматично працює** для ру локалі
- ⚠️ **Увага:** Може потрібно оновити очікувані тексти меню для ру локалі

### **Тест-кейс #3: Language switcher / Localization ✅**
- Перевіряє перемикач мови
- **ДОДАНО:** Перевірка перекладу меню
- **ДОДАНО:** Відправка форми на ру локалі
- **Автоматично працює** для ру локалі

### **Тест-кейс #4: Hero block ✅**
- Перевіряє Hero секцію
- **Автоматично працює** для ру локалі
- ⚠️ **Увага:** Може потрібно оновити очікуваний текст CTA кнопки для ру локалі

### **Тест-кейс #5: Modal "Записатися на курс" ✅**
- Перевіряє модальну форму
- **Автоматично працює** для ру локалі
- ⚠️ **Увага:** Може потрібно оновити очікувані тексти форми для ру локалі

---

## ⚠️ Що може потребувати уваги

### 1. **Очікувані тексти в тестах**

Деякі тести перевіряють конкретні тексти (наприклад, "Записатися на курс"). На російській локалі цей текст може бути іншим.

**Рішення:**
- Додати в `pages.json` поле `expected.ctaText` для кожної локалі
- Оновити тести, щоб вони використовували правильний текст з конфігу

**Приклад:**

```json
{
  "key": "ai-marketing-specialist-ru",
  "locale": "ua-ru",
  "expected": {
    "h1": "AI Automator",
    "ctaText": "Записаться на курс",  // Російська версія
    "modalTitle": "Записаться на курс"
  }
}
```

### 2. **Перевірка перекладу контенту**

Зараз тести перевіряють структуру, але не перевіряють, чи контент дійсно перекладений.

**Можна додати:**
- Перевірку, що H1 текст змінився (якщо він перекладений)
- Перевірку перекладу кнопок, форм, меню

### 3. **Zoho product mapping**

Перевірка Zoho product mapping в тесті форми може потребувати оновлення, якщо для ру локалі використовуються інші product_id.

**Перевірити:**
- Чи правильний `productId` для ру локалі в `pages.json`
- Чи відправляється правильний `product_id` при відправці форми на ру локалі

---

## 📊 Структура pages.json

```json
[
  // Українська локаль
  {
    "key": "ai-marketing-specialist",
    "url": "https://goit.global/ua/courses/ai-marketing-specialist/",
    "locale": "ua"
  },
  
  // Російська локаль
  {
    "key": "ai-marketing-specialist-ru",
    "url": "https://goit.global/ua-ru/courses/ai-marketing-specialist/",
    "locale": "ua-ru"
  },
  
  // ... і так далі для інших курсів
]
```

**Важливо:** Кожен курс має **два записи** - для української та російської локалей.

---

## 📝 Підсумок

✅ **Додано:**
- Відправка форми на ру локалі в `03-language-switcher.cy.js`
- Російські локалі в `pages.json`
- Фільтрація по локалі в усіх тестах через `LOCALE` env змінну
- npm scripts для окремого запуску кожної локалі

✅ **Результат:**
- Можна тестувати укр та ру локалі **окремо**
- Можна тестувати обидві локалі разом (за замовчуванням)
- Легко додати нові курси з обома локалями
- Зручні npm scripts для швидкого запуску

⚠️ **Може потребувати уваги:**
- Очікувані тексти в тестах (CTA кнопки, форми) - можуть відрізнятись на ру локалі
- Zoho product mapping для ру локалі - перевірити, чи правильний product_id

---

## 🎓 Висновок

Тепер можна **тестувати українську та російську локалі окремо**:
- `npm run test:ua` - тільки укр локаль
- `npm run test:ru` - тільки ру локаль
- `npm run cy:run` - обидві локалі разом

Це дозволяє швидко знаходити проблеми, специфічні для кожної локалі! 🚀

