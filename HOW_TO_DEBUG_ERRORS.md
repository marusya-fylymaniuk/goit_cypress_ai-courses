# 🔍 Як розбирати помилки в тестах (для новачків)

Цей гайд допоможе тобі зрозуміти, що означають помилки в тестах і як їх виправити.

---

## 📋 Крок 1: Знайди помилку в терміналі

Коли тести падають, в терміналі з'являється текст помилки. Він зазвичай виглядає так:

```
✖ [ai-marketing-specialist] Modal "Записатися на курс" (форма) -- submits form (REAL lead) and shows thank-you; verifies Zoho product mapping (12345ms)
   AssertionError: expected '...' to include '...'
```

**Що шукати:**
- Назва тесту (наприклад, "submits form")
- Назва сторінки (наприклад, "[ai-marketing-specialist]")
- Текст помилки (наприклад, "AssertionError", "Timed out")

---

## 📋 Крок 2: Зрозумій тип помилки

### Тип 1: AssertionError (Помилка перевірки)

**Що це означає:** Тест очікував одне, а отримав інше.

**Приклад:**
```
AssertionError: expected 'AI Automator' to equal 'AI Marketing Specialist'
```

**Що робити:**
1. Подивись, що очікувалося (expected)
2. Подивись, що отримано (actual)
3. Виправ дані в `pages.json` або на сайті

---

### Тип 2: Timed out (Час вийшов)

**Що це означає:** Тест чекав на щось, але воно не з'явилося за 10-30 секунд.

**Приклад:**
```
Timed out retrying after 30000ms: Expected to find element: '[data-messages]', but never found it.
```

**Що робити:**
1. Перевір, чи елемент існує на сторінці
2. Перевір, чи правильний селектор
3. Можливо, елемент з'являється пізніше - збільш timeout

---

### Тип 3: Element not found (Елемент не знайдено)

**Що це означає:** Тест шукав елемент на сторінці, але не знайшов його.

**Приклад:**
```
Expected to find element: '#benefits', but never found it.
```

**Що робити:**
1. Перевір, чи елемент існує на сторінці (відкрий сторінку в браузері)
2. Перевір, чи правильний селектор (можливо, змінився HTML)
3. Можливо, елемент прихований - перевір CSS

---

### Тип 4: Network error (Помилка мережі)

**Що це означає:** Запит до сервера не вдався.

**Приклад:**
```
NetworkError: Failed to fetch
POST https://goit.global/wp/wp-admin/admin-post.php
```

**Що робити:**
1. Перевір, чи сайт доступний
2. Перевір, чи сервер працює
3. Можливо, потрібна авторизація

---

## 📋 Крок 3: Знайди, де помилка в коді

Кожна помилка має "stack trace" - список файлів і рядків, де сталася помилка:

```
at Context.eval (cypress/e2e/05-modal-form.cy.js:534:15)
```

**Що це означає:**
- `05-modal-form.cy.js` - файл з тестом
- `534` - номер рядка, де помилка
- `15` - позиція в рядку

**Що робити:**
1. Відкрий файл тесту
2. Знайди рядок з помилкою
3. Подивись, що там перевіряється

---

## 📋 Крок 4: Розбери конкретну помилку

### Помилка 1: "Expected to find element: '[data-messages]'"

**Що означає:** Тест шукає елемент з атрибутом `data-messages`, але не знаходить його.

**Можливі причини:**
1. Елемент не існує на сторінці
2. Елемент прихований (має клас `hidden`)
3. Елемент з'являється пізніше (потрібно чекати)

**Як виправити:**
1. Відкрий сторінку в браузері
2. Відкрий DevTools (F12)
3. Знайди елемент `[data-messages]`
4. Якщо його немає - це проблема на сайті
5. Якщо він є, але прихований - перевір, чи він стає видимим після відправки форми

---

### Помилка 2: "Timed out waiting for '@leadSubmit'"

**Що означає:** Тест чекав на відправку форми, але форма не відправилася.

**Можливі причини:**
1. Форма не валідна (незаповнені обов'язкові поля)
2. JavaScript помилка на сторінці
3. Сервер не відповідає

**Як виправити:**
1. Перевір, чи всі поля заповнені
2. Перевір консоль браузера на помилки JavaScript
3. Перевір, чи сервер працює

---

### Помилка 3: "Expected 'X' to equal 'Y'"

**Що означає:** Тест очікував значення 'X', але отримав 'Y'.

**Приклад:**
```
Expected 'AI Automator' to equal 'AI Marketing Specialist'
```

**Можливі причини:**
1. Неправильні дані в `pages.json`
2. Змінився текст на сайті
3. Неправильна локаль (ua vs ua-ru)

**Як виправити:**
1. Перевір дані в `cypress/fixtures/pages.json`
2. Перевір, що на сторінці правильний текст
3. Онови дані в `pages.json`, якщо потрібно

---

### Помилка 4: "Expected to find element: '#benefits'"

**Що означає:** Тест шукає секцію з id `#benefits`, але не знаходить її.

**Можливі причини:**
1. Секція не існує на цій сторінці
2. Секція має інший id
3. Секція прихована

**Як виправити:**
1. Відкрий сторінку в браузері
2. Перевір, чи є секція `#benefits`
3. Якщо її немає - видали з `pages.json` в `anchors` або додай на сайт

---

## 📋 Крок 5: Виправ помилку

Після того, як зрозумів причину помилки:

1. **Якщо помилка в даних:**
   - Онови `cypress/fixtures/pages.json`
   - Перевір, чи всі дані правильні

2. **Якщо помилка на сайті:**
   - Повідом розробників
   - Або зроби тест опціональним (якщо елемент не обов'язковий)

3. **Якщо помилка в тесті:**
   - Виправ код тесту
   - Додай timeout, якщо елемент з'являється пізніше
   - Зміни селектор, якщо HTML змінився

---

## 💡 Поради

1. **Завжди читай повний текст помилки** - він містить важливу інформацію
2. **Дивись на скріншоти** - вони показують, як виглядала сторінка в момент помилки
3. **Перевіряй в браузері** - відкрий сторінку і подивись, чи все на місці
4. **Використовуй DevTools** - F12 допоможе знайти елементи на сторінці

---

## 🆘 Якщо нічого не допомагає

1. Скопіюй повний текст помилки
2. Відкрий скріншот помилки
3. Подивись на код тесту в місці помилки
4. Спробуй зрозуміти, що очікував тест і що отримав

**Пам'ятай:** Кожна помилка - це можливість навчитися чомусь новому! 😊



