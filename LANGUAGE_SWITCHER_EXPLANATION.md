# 📖 Детальне пояснення тест-кейсу #3: Language switcher / Localization

## 🎯 Що це за тест-кейс?

Цей тест-кейс перевіряє, чи правильно працює **перемикач мови** на сайті. На сайті GoIT є дві мови:
- **Українська** (`/ua/`) 
- **Російська** (`/ua-ru/`)

Тести перевіряють, що користувач може переключатися між мовами, і що все працює коректно.

---

## 🛠️ Що я зробив (технічна частина)

### 1. **Створив допоміжні функції (helpers)**

Це як "інструменти", які допомагають тестам працювати:

#### `getLocaleFromUrl(url)` - Визначає мову з URL
```javascript
// Приклад:
// URL: "https://goit.global/ua/courses/ai-marketing-specialist/"
// Повертає: "ua" (українська)

// URL: "https://goit.global/ua-ru/courses/ai-marketing-specialist/"
// Повертає: "ua-ru" (російська)
```

**Простими словами:** Дивиться на адресу сторінки і каже: "Це українська сторінка" або "Це російська сторінка".

#### `getAlternateLocale(currentLocale)` - Знаходить протилежну мову
```javascript
// Якщо зараз "ua" → повертає "ua-ru"
// Якщо зараз "ua-ru" → повертає "ua"
```

**Простими словами:** Якщо ти на українській сторінці, каже "альтернативна мова - російська", і навпаки.

#### `buildUrlWithLocale(originalUrl, targetLocale)` - Створює URL з іншою мовою
```javascript
// Вхід: "https://goit.global/ua/courses/ai-marketing-specialist/", "ua-ru"
// Вихід: "https://goit.global/ua-ru/courses/ai-marketing-specialist/"
```

**Простими словами:** Бере адресу української сторінки і робить з неї адресу російської сторінки (або навпаки).

---

## ✅ Які перевірки я зробив (6 тестів)

### **Тест 1: "language switcher exists in header and is visible"**
**Що перевіряє:** Чи є перемикач мови в header і чи він видимий?

**Як працює:**
1. Шукає в `<header>` елементи, які можуть бути перемикачем мови:
   - Посилання з `/ua/` або `/ua-ru/` в адресі
   - Кнопки з атрибутами `data-lang` або `data-locale`
   - Кнопки з текстом про мову (aria-label)
2. Перевіряє, що знайшов хоча б один видимий елемент

**Чому це важливо:** Якщо перемикача мови немає або він прихований, користувач не зможе змінити мову.

---

### **Тест 2: "can navigate to alternate locale and URL changes correctly"**
**Що перевіряє:** Чи можна перейти на альтернативну мову і чи правильно змінюється URL?

**Як працює:**
1. Визначає поточну мову (наприклад, `ua`)
2. Знаходить альтернативну мову (`ua-ru`)
3. Створює URL для альтернативної мови
4. Переходить на цей URL
5. Перевіряє:
   - URL містить правильну мову (`/ua-ru/`)
   - URL точно такий, як очікувалось
   - Сторінка завантажилась (є `<body>` і `<h1>`)

**Простими словами:** 
- Ти на українській сторінці
- Переходиш на російську версію
- Перевіряєш, що адреса змінилась правильно і сторінка відкрилась

**Чому це важливо:** Перевіряє, що альтернативна мова взагалі існує і доступна.

---

### **Тест 3: "page content updates after locale change (H1 remains visible)"**
**Що перевіряє:** Чи оновлюється контент після зміни мови, і чи залишається H1 видимим?

**Як працює:**
1. Запам'ятовує текст H1 на поточній мові
2. Переходить на альтернативну мову
3. Перевіряє, що H1 все ще є і видимий (текст може змінитись, але елемент має бути)

**Простими словами:**
- Ти бачиш заголовок "AI Automator" на українській
- Переходиш на російську
- Перевіряєш, що заголовок все ще є (може бути інший текст, але він має бути)

**Чому це важливо:** Перевіряє, що структура сторінки не зламалась після зміни мови.

---

### **Тест 4: "alternate locale page works on mobile viewport"**
**Що перевіряє:** Чи працює альтернативна мова на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375x667 пікселів)
2. Переходить на альтернативну мову
3. Перевіряє, що сторінка завантажилась і H1 видимий

**Простими словами:** 
- Відкриваєш сайт на телефоні
- Переходиш на іншу мову
- Перевіряєш, що все працює

**Чому це важливо:** Багато користувачів заходять з телефонів, тому важливо перевірити, що перемикач мови працює і на мобільних.

---

### **Тест 5: "locale change preserves course page path (only locale prefix changes)"**
**Що перевіряє:** Чи зберігається правильна структура URL після зміни мови?

**Як працює:**
1. Переходить на альтернативну мову
2. Перевіряє, що:
   - Змінився тільки префікс мови (`/ua/` → `/ua-ru/`)
   - Залишився шлях до курсу (`/courses/`)
   - Залишився ключ сторінки (`ai-marketing-specialist`)

**Простими словами:**
- Було: `/ua/courses/ai-marketing-specialist/`
- Стало: `/ua-ru/courses/ai-marketing-specialist/`
- Перевіряєш, що змінилась тільки мова, а не вся адреса

**Чому це важливо:** Якщо структура URL зламається, користувач може потрапити не на ту сторінку.

---

### **Тест 6: "can navigate back to original locale (round-trip)"**
**Що перевіряє:** Чи можна повернутися назад на оригінальну мову?

**Як працює:**
1. Переходить на альтернативну мову
2. Перевіряє, що URL правильний
3. Повертається на оригінальну мову
4. Перевіряє, що повернувся на правильну адресу і H1 видимий

**Простими словами:**
- Ти на українській → перейшов на російську → повернувся на українську
- Перевіряєш, що все працює в обидві сторони

**Чому це важливо:** Користувач має мати можливість повернутися назад, якщо помилився.

---

**Примітка:** Спочатку був тест відправки форми на ру локалі, але його прибрали, оскільки відправка форми - це частина тест-кейсу #5 (Modal form), який автоматично працює для обох локалей.

---

## 🎨 Data-driven підхід

**Що це означає:**
Тести автоматично запускаються для **всіх сторінок** з файлу `pages.json`:
- `ai-marketing-specialist`
- `ai-content-maker`
- (і будь-які інші, які додадуться в майбутньому)

**Чому це добре:**
- Не потрібно писати тести для кожної сторінки окремо
- Додав нову сторінку в `pages.json` → тести автоматично працюють для неї

---

## 💡 Які перевірки можна додати (опціонально)

### 1. **Перевірка перекладу контенту**
```javascript
it('H1 text is translated correctly', () => {
  // Запам'ятовує H1 на українській
  // Переходить на російську
  // Перевіряє, що H1 змінився (не той самий текст)
});
```
**Чому:** Перевіряє, що контент дійсно перекладений, а не просто змінилась мова в URL.

### 2. **Перевірка перекладу кнопок**
```javascript
it('CTA button text is translated', () => {
  // Перевіряє, що кнопка "Записатися на курс" має правильний переклад
  // На російській може бути інший текст
});
```

### 3. **Перевірка мета-тегів (title, description)**
```javascript
it('meta tags are translated', () => {
  // Перевіряє, що <title> і <meta name="description"> змінились
});
```

### 4. **Перевірка форми в модалці**
```javascript
it('modal form labels are translated', () => {
  // Відкриває форму на українській
  // Переходить на російську
  // Відкриває форму знову
  // Перевіряє, що плейсхолдери і лейбли перекладені
});
```

### 5. **Перевірка навігаційного меню**
```javascript
it('navigation menu items are translated', () => {
  // Перевіряє, що пункти меню ("Всі AI курси", "QA + AI") перекладені
});
```

### 6. **Перевірка збереження стану (якщо є)**
```javascript
it('user preferences are preserved after language switch', () => {
  // Якщо користувач щось вибрав (наприклад, фільтри)
  // Перевіряє, що це збереглось після зміни мови
});
```

### 7. **Перевірка cookie/localStorage**
```javascript
it('language preference is saved', () => {
  // Перевіряє, що вибір мови зберігається в cookie або localStorage
  // І при наступному відвідуванні користувач бачить правильну мову
});
```

### 8. **Перевірка аналітики (dataLayer)**
```javascript
it('language switch triggers analytics event', () => {
  // Перевіряє, що при зміні мови відправляється подія в dataLayer
  // Наприклад: { event: 'language_switch', locale: 'ua-ru' }
});
```

---

## 🔍 Чому я використав прямий перехід на URL, а не клік на перемикач?

**Проблема, з якою я зіткнувся:**
- Спочатку я намагався знайти і клікнути на перемикач мови
- Але він перенаправляв на іншу сторінку (`/courses/recruitment/`) замість зміни мови
- Це означає, що перемикач мови може працювати інакше, ніж я очікував

**Рішення:**
Я вирішив перевіряти **навігацію між локалями через прямі переходи на URL**. Це:
- ✅ Більш стабільно (не залежить від UI елементів)
- ✅ Перевіряє саме те, що важливо: чи існує альтернативна мова і чи вона доступна
- ✅ Менше схильне до змін в UI

**Якщо потрібно перевірити саме клік на перемикач:**
Можна додати окремий тест, який:
1. Знаходить перемикач мови (якщо він є)
2. Клікає на нього
3. Перевіряє, що URL змінився правильно

---

## 📊 Результати

**Що працює:**
- ✅ 12 тестів успішно проходять (6 для кожної сторінки)
- ✅ Тести data-driven (працюють для всіх сторінок автоматично)
- ✅ Перевіряють основні сценарії зміни мови

**Що можна покращити:**
- Додати перевірки перекладу контенту (якщо потрібно)
- Додати перевірку кліку на перемикач (якщо він працює правильно)
- Додати перевірки аналітики та збереження стану

---

## 🎓 Висновок

Цей тест-кейс перевіряє **основні аспекти локалізації**:
1. Перемикач мови існує і видимий
2. Можна перейти на альтернативну мову
3. URL змінюється правильно
4. Сторінка завантажується коректно
5. Структура URL зберігається
6. Працює на мобільних пристроях
7. Можна повернутися назад

Це **мінімальний набір перевірок**, який гарантує, що локалізація працює базово правильно. Додаткові перевірки можна додати за потреби.

