# 🎨 Визуальные примеры для понимания

## 📊 Как работает Cypress (схема)

```
┌─────────────────────────────────────────────────┐
│  Твой тест (02-header-navigation.cy.js)         │
│                                                  │
│  cy.visit('/')  ────────────────┐               │
│                                  │               │
│  cy.get('button') ───────────────┼───┐            │
│                                  │   │            │
│  .click() ───────────────────────┼───┼───┐        │
│                                  │   │   │        │
│  .should('be.visible') ──────────┼───┼───┼───┐    │
└──────────────────────────────────┼───┼───┼───┼────┘
                                   │   │   │   │
                                   ▼   ▼   ▼   ▼
┌─────────────────────────────────────────────────┐
│         Cypress (робот-тестировщик)             │
│                                                  │
│  • Открывает браузер                            │
│  • Выполняет команды                             │
│  • Проверяет результаты                         │
└─────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────┐
│         Реальный сайт (goit.global)             │
│                                                  │
│  • Загружается страница                         │
│  • Пользователь видит кнопки                    │
│  • Cypress кликает на них                       │
└─────────────────────────────────────────────────┘
```

---

## 🔄 Поток выполнения теста (пример)

Давай проследим, как выполняется тест "Обрати професію":

```
ШАГ 1: Открываем страницу
┌─────────────────────────────────────┐
│ cy.visit('/')                       │
│                                     │
│ Результат: Страница загружена      │
└─────────────────────────────────────┘
              │
              ▼
ШАГ 2: Создаём dataLayer
┌─────────────────────────────────────┐
│ cy.window().then((win) => {         │
│   if (!win.dataLayer) {            │
│     win.dataLayer = [];            │
│   }                                 │
│ })                                  │
│                                     │
│ Результат: dataLayer = []          │
└─────────────────────────────────────┘
              │
              ▼
ШАГ 3: Ждём загрузки скриптов
┌─────────────────────────────────────┐
│ cy.wait(1000)                       │
│                                     │
│ Результат: Прошла 1 секунда        │
└─────────────────────────────────────┘
              │
              ▼
ШАГ 4: Ищем кнопку
┌─────────────────────────────────────┐
│ cy.get('body').then(($body) => {   │
│   const desktopBtn = ...            │
│   const mobileBtn = ...            │
│   const targetBtn = ...            │
│ })                                  │
│                                     │
│ Результат: Нашли кнопку!           │
└─────────────────────────────────────┘
              │
              ▼
ШАГ 5: Проверяем атрибут
┌─────────────────────────────────────┐
│ .should('have.attr', 'target',     │
│          '_blank')                  │
│                                     │
│ Результат: ✅ Атрибут есть         │
└─────────────────────────────────────┘
              │
              ▼
ШАГ 6: Кликаем
┌─────────────────────────────────────┐
│ .click()                            │
│                                     │
│ Результат: Кнопка нажата            │
│          ↓                          │
│    Сайт записывает событие:        │
│    dataLayer.push({                 │
│      event: 'register_click'         │
│    })                               │
└─────────────────────────────────────┘
              │
              ▼
ШАГ 7: Проверяем результат
┌─────────────────────────────────────┐
│ cy.window().then((win) => {         │
│   const dataLayer = win.dataLayer; │
│   const hasEvent = dataLayer.some( │
│     item => item.event ===          │
│     'register_click'                │
│   );                                │
│   expect(hasEvent).to.be.true;      │
│ })                                  │
│                                     │
│ Результат: ✅ Событие найдено!     │
│            Тест пройден!            │
└─────────────────────────────────────┘
```

---

## 🎯 Пример: Поиск кнопки (до и после)

### ❌ БЫЛО (не работало):

```javascript
// Проблема: искали все кнопки, включая невидимые
cy.contains('a, button', 'Обрати професію').click();
// Ошибка: "element is not visible"
```

**Что происходило:**
```
┌─────────────────────────────────────┐
│ Все кнопки "Обрати професію":      │
│                                     │
│  [btn-prof-1] ← видимая (desktop)   │
│  [btn-prof-2] ← скрыта (mobile)    │
│                                     │
│ Cypress пытается кликнуть на       │
│ скрытую кнопку → ОШИБКА!           │
└─────────────────────────────────────┘
```

### ✅ СТАЛО (работает):

```javascript
// Решение: ищем только видимые кнопки
const desktopBtn = $body.find('a.btn-prof-1').filter(':visible');
const mobileBtn = $body.find('a.btn-prof-2').filter(':visible');
const targetBtn = desktopBtn.length > 0 ? desktopBtn.first() : mobileBtn.first();
```

**Что происходит:**
```
┌─────────────────────────────────────┐
│ Шаг 1: Ищем desktop кнопку        │
│   [btn-prof-1] ← найдена! ✅       │
│                                     │
│ Шаг 2: Проверяем видимость        │
│   .filter(':visible')               │
│   [btn-prof-1] ← видимая ✅        │
│                                     │
│ Шаг 3: Выбираем её                │
│   targetBtn = desktopBtn.first()   │
│                                     │
│ Шаг 4: Кликаем                    │
│   .click() → УСПЕХ! ✅             │
└─────────────────────────────────────┘
```

---

## 🔍 Как работают селекторы (примеры)

### Пример 1: Поиск по тегу
```javascript
cy.get('button')
```
**Находит:** Все элементы `<button>` на странице

```
HTML:
<button>Кнопка 1</button>
<button>Кнопка 2</button>
<a>Ссылка</a>

Результат: Найдено 2 кнопки
```

### Пример 2: Поиск по классу
```javascript
cy.get('.btn-prof-1')
```
**Находит:** Все элементы с классом `btn-prof-1`

```
HTML:
<a class="btn-prof-1">Desktop кнопка</a>
<a class="btn-prof-2">Mobile кнопка</a>
<a class="btn-prof-1">Ещё desktop</a>

Результат: Найдено 2 элемента
```

### Пример 3: Поиск по тексту
```javascript
cy.contains('Обрати професію')
```
**Находит:** Элемент, который содержит этот текст

```
HTML:
<a>Обрати професію</a>
<button>Другая кнопка</button>
<span>Обрати професію здесь</span>

Результат: Найдено 2 элемента (a и span)
```

### Пример 4: Комбинация (тег + класс)
```javascript
cy.get('a.btn-prof-1')
```
**Находит:** Только ссылки (`<a>`) с классом `btn-prof-1`

```
HTML:
<a class="btn-prof-1">✅ Найдено</a>
<button class="btn-prof-1">❌ Не найдено (не <a>)</button>
<a class="btn-prof-2">❌ Не найдено (другой класс)</a>

Результат: Найдено 1 элемент
```

### Пример 5: Фильтр по видимости
```javascript
cy.get('a.btn-prof-1').filter(':visible')
```
**Находит:** Только видимые элементы

```
HTML:
<a class="btn-prof-1" style="display: block">✅ Видимая</a>
<a class="btn-prof-1" style="display: none">❌ Скрытая</a>
<a class="btn-prof-1" class="hidden">❌ Скрытая</a>

Результат: Найдено 1 элемент (только видимая)
```

---

## 📦 Что такое dataLayer (простыми словами)

Представь, что `dataLayer` — это **дневник событий**:

```
┌─────────────────────────────────────┐
│ dataLayer = []  (пустой дневник)    │
└─────────────────────────────────────┘
              │
              ▼
    Пользователь кликает на кнопку
              │
              ▼
┌─────────────────────────────────────┐
│ dataLayer.push({                    │
│   event: 'register_click'           │
│ })                                   │
│                                     │
│ Теперь дневник содержит:            │
│ [                                    │
│   { event: 'register_click' }      │
│ ]                                    │
└─────────────────────────────────────┘
              │
              ▼
    Наш тест проверяет:
┌─────────────────────────────────────┐
│ const hasEvent = dataLayer.some(    │
│   item => item.event ===            │
│   'register_click'                  │
│ );                                  │
│                                     │
│ Результат: true ✅                 │
│ "Событие записано в дневник!"      │
└─────────────────────────────────────┘
```

---

## 🎭 Разница между desktop и mobile версиями

### Desktop версия (большой экран):
```
┌─────────────────────────────────────┐
│  [Лого]  [Меню]  [Обрати професію] │
│                                     │
│  Кнопка: a.btn-prof-1              │
│  Видимость: ✅ видимая             │
└─────────────────────────────────────┘
```

### Mobile версия (маленький экран):
```
┌─────────────────────┐
│ [☰] [Лого]          │
│                     │
│ [Обрати професію]   │
│                     │
│ Кнопка: a.btn-prof-2│
│ Видимость: ✅ видимая│
└─────────────────────┘
```

**Проблема:** На desktop `btn-prof-2` скрыта, на mobile `btn-prof-1` может быть скрыта.

**Решение:** Ищем обе версии и выбираем видимую!

---

## 🔄 Асинхронность (почему нужен .then())

### ❌ Неправильно (не работает):
```javascript
const button = cy.get('button');  // ❌ Не работает!
button.click();                    // ❌ button ещё не найден!
```

**Почему не работает:**
```
Шаг 1: cy.get('button') начинает поиск...
       (но поиск ещё не закончен!)
       
Шаг 2: button.click() пытается кликнуть...
       (но button ещё undefined!)

Результат: ОШИБКА!
```

### ✅ Правильно (работает):
```javascript
cy.get('button').then(($button) => {
  // Код здесь выполнится ПОСЛЕ того, как кнопка найдена
  $button.click();
});
```

**Почему работает:**
```
Шаг 1: cy.get('button') начинает поиск...
       
Шаг 2: Ждём, пока поиск закончится...
       
Шаг 3: Кнопка найдена! ✅
       
Шаг 4: Выполняем код в .then()
       $button.click() → УСПЕХ! ✅
```

---

## 🎪 Пример: Полный тест с объяснениями

```javascript
it('should open "Обрати професію" in new tab', () => {
  // ┌─────────────────────────────────────────┐
  // │ ШАГ 1: Подготовка                       │
  // │ Создаём dataLayer, если его нет        │
  // └─────────────────────────────────────────┘
  cy.window().then((win) => {
    // win = окно браузера
    // win.dataLayer = переменная для аналитики
    
    if (!win.dataLayer) {
      // Если dataLayer не существует...
      win.dataLayer = [];
      // ...создаём пустой массив
    }
  });
  
  // ┌─────────────────────────────────────────┐
  // │ ШАГ 2: Ждём загрузки                    │
  // │ Даём время скриптам загрузиться         │
  // └─────────────────────────────────────────┘
  cy.wait(1000); // 1000 миллисекунд = 1 секунда
  
  // ┌─────────────────────────────────────────┐
  // │ ШАГ 3: Ищем кнопку                      │
  // │ Ищем body, потом внутри него кнопки     │
  // └─────────────────────────────────────────┘
  cy.get('body').then(($body) => {
    // $body = элемент <body> на странице
    
    // Ищем desktop версию
    const desktopBtn = $body.find('a.btn-prof-1').filter(':visible');
    // desktopBtn = массив найденных кнопок (0 или больше)
    
    // Ищем mobile версию
    const mobileBtn = $body.find('a.btn-prof-2').filter(':visible');
    
    // Выбираем ту, которая видна
    const targetBtn = desktopBtn.length > 0 
      ? desktopBtn.first()  // Если desktop есть → берём её
      : mobileBtn.first(); // Иначе → берём mobile
    
    // ┌─────────────────────────────────────────┐
    // │ ШАГ 4: Проверяем и кликаем            │
    // └─────────────────────────────────────────┘
    if (targetBtn.length > 0) {
      // Если нашли кнопку...
      
      cy.wrap(targetBtn).then(($btn) => {
        // Превращаем jQuery элемент в Cypress элемент
        
        // Проверяем атрибут target="_blank"
        cy.wrap($btn).should('have.attr', 'target', '_blank');
        // Это значит: "У кнопки должен быть атрибут target со значением _blank"
        
        // Кликаем
        cy.wrap($btn).click();
        // Cypress кликает на кнопку (как пользователь)
        
        // Ждём, пока событие запишется
        cy.wait(500);
        
        // ┌─────────────────────────────────────────┐
        // │ ШАГ 5: Проверяем результат            │
        // └─────────────────────────────────────────┘
        cy.window().then((win) => {
          // Снова получаем доступ к окну браузера
          
          const dataLayer = win.dataLayer || [];
          // dataLayer = массив событий (или пустой массив, если нет)
          
          const hasRegisterClick = dataLayer.some(item => 
            item.event === 'register_click'
          );
          // Проверяем: есть ли в массиве событие с event: 'register_click'
          // .some() возвращает true, если хотя бы один элемент подходит
          
          expect(hasRegisterClick).to.be.true;
          // Проверяем: должно быть true
          // Если false → тест упадёт с ошибкой
        });
      });
    }
  });
});
```

---

## 🎓 Ключевые моменты для запоминания

1. **Cypress команды выполняются последовательно** (одна за другой)
2. **`.then()` = "потом"** (код выполнится после предыдущей команды)
3. **Всегда проверяй видимость** перед кликом
4. **`dataLayer`** — это массив событий для аналитики
5. **`.filter(':visible')`** — оставляет только видимые элементы
6. **`cy.wrap()`** — превращает jQuery элемент в Cypress элемент
7. **`expect(...).to.be.true`** — проверка (если false → тест упадёт)

---

Надеюсь, теперь всё понятно! 🚀

