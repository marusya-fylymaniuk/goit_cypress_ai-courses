# 📚 Детальне пояснення всіх тест-кейсів (для новачків)

Цей документ пояснює **кожну перевірку** в наших тестах простими словами, як для людини, яка ніколи не програмувала.

---

## 🎯 Що таке тести?

**Тести** - це автоматичні перевірки, які роблять те саме, що робить людина, коли перевіряє сайт:
- Відкриває сторінку
- Клікає на кнопки
- Перевіряє, чи все працює правильно

**Навіщо це потрібно?**
- Щоб не перевіряти все вручну кожного разу
- Щоб знайти помилки швидше
- Щоб переконатися, що сайт працює правильно

---

## 📋 Тест-кейс #1: Smoke / Sanity / SEO checks

**Що це означає?** "Smoke" = дим. Якщо є дим, значить щось горить. Якщо ці базові тести проходять, значить сайт "не горить" і можна тестувати далі.

### ✅ Перевірка 1: HTTP redirects to HTTPS

**Що перевіряє:** Чи перенаправляє сайт з небезпечного HTTP на безпечний HTTPS?

**Як працює:**
1. Тест намагається відкрити сторінку через `http://` (небезпечний протокол)
2. Перевіряє, чи сервер відповідає кодом 301, 302, 307 або 308 (це коди, що означають "перенаправлення")
3. Перевіряє, чи в заголовку `Location` є адреса з `https://`
4. Перевіряє, чи ця адреса точно така ж, як очікувалась

**Навіщо це потрібно:** Безпека. Всі сучасні сайти мають працювати через HTTPS (зашифроване з'єднання).

**Простими словами:** Якщо хтось намагається зайти через `http://`, сайт має автоматично перенаправити на `https://`.

---

### ✅ Перевірка 2: no redirects or errors when opening the course page (server-level)

**Що перевіряє:** Чи відкривається сторінка курсу безпосередньо, без додаткових перенаправлень?

**Як працює:**
1. Тест відкриває сторінку курсу
2. Перевіряє, чи сервер відповідає кодом 200 (успіх)
3. Перевіряє, чи немає заголовка `Location` (який означає перенаправлення)

**Навіщо це потрібно:** Якщо сторінка перенаправляє, це може означати проблему з налаштуванням сервера.

**Простими словами:** Сторінка має відкриватися одразу, без "стрибків" на інші адреси.

---

### ✅ Перевірка 3: browser lands on the exact course URL (no client-side redirect)

**Що перевіряє:** Чи залишається браузер на правильній адресі після завантаження сторінки?

**Як працює:**
1. Тест відкриває сторінку
2. Перевіряє, чи адреса в браузері точно така, як очікувалась
3. Перевіряє, чи немає JavaScript-перенаправлень

**Навіщо це потрібно:** Іноді JavaScript може змінити адресу після завантаження, що може бути проблемою.

**Простими словами:** Адреса в браузері має залишатись такою, яку ти відкрив.

---

### ✅ Перевірка 4: server response is 200

**Що перевіряє:** Чи сервер відповідає успішно?

**Як працює:**
1. Тест запитує сторінку з сервера
2. Перевіряє, чи код відповіді = 200 (успіх)

**Навіщо це потрібно:** Код 200 означає "все добре". Інші коди (404, 500) означають помилки.

**Простими словами:** Сервер має відповісти "200 - все добре", а не "404 - не знайдено" або "500 - помилка".

---

### ✅ Перевірка 5: page loads (body visible; document complete)

**Що перевіряє:** Чи завантажилась сторінка повністю?

**Як працює:**
1. Перевіряє, чи є елемент `<body>` і чи він видимий
2. Перевіряє, чи документ завантажився повністю (`readyState === 'complete'`)

**Навіщо це потрібно:** Якщо сторінка не завантажилась, інші тести не матимуть сенсу.

**Простими словами:** Сторінка має повністю завантажитись, а не "висіти" в процесі завантаження.

---

### ✅ Перевірка 6: canonical exists and matches browser URL (with trailing slash)

**Що перевіряє:** Чи є правильний canonical URL?

**Що таке canonical URL?** Це спеціальний тег, який каже пошуковим системам: "Це основна версія цієї сторінки".

**Як працює:**
1. Шукає тег `<link rel="canonical">` в HTML
2. Перевіряє, чи він існує
3. Перевіряє, чи він закінчується на `/` (trailing slash)
4. Перевіряє, чи він точно такий же, як адреса в браузері

**Навіщо це потрібно:** Для SEO (пошукова оптимізація). Пошукові системи використовують canonical, щоб знати, яка версія сторінки основна.

**Простими словами:** Сторінка має мати "канонічну" адресу, яка точно відповідає адресі в браузері.

---

### ✅ Перевірка 7: URL has no underscores, spaces, or Cyrillic characters

**Що перевіряє:** Чи адреса сторінки "чиста" (без підкреслень, пробілів, кирилиці)?

**Як працює:**
1. Бере адресу сторінки
2. Перевіряє, чи немає в ній:
   - Підкреслень (`_`)
   - Пробілів (` `)
   - Кириличних букв (а, б, в, г...)

**Навіщо це потрібно:** 
- Для SEO (пошукові системи краще індексують "чисті" URL)
- Для сумісності (деякі системи не розуміють кирилицю в URL)

**Простими словами:** Адреса має бути типу `/courses/ai-marketing-specialist/`, а не `/courses/ai_marketing_specialist/` або `/courses/ай-маркетинг/`.

---

### ✅ Перевірка 8: no meta robots noindex/nofollow

**Що перевіряє:** Чи не заблокована сторінка для пошукових систем?

**Що таке meta robots?** Це спеціальний тег, який каже пошуковим системам, як індексувати сторінку.

**Як працює:**
1. Шукає тег `<meta name="robots">`
2. Якщо він є, перевіряє, чи в ньому немає:
   - `noindex` (не індексувати сторінку)
   - `nofollow` (не переходити по посиланням)

**Навіщо це потрібно:** Якщо сторінка має `noindex`, вона не з'явиться в пошуку Google.

**Простими словами:** Сторінка має бути доступна для пошукових систем, а не заблокована.

---

### ✅ Перевірка 9: all images have alt attribute

**Що перевіряє:** Чи всі зображення мають атрибут `alt`?

**Що таке `alt`?** Це текст, який описує зображення. Він потрібен для:
- Сліпих людей (спеціальні програми читають цей текст)
- SEO (пошукові системи бачать цей текст)

**Як працює:**
1. Знаходить всі теги `<img>` на сторінці
2. Перевіряє, чи кожен має атрибут `alt`
3. Якщо хоча б одне зображення не має `alt`, тест падає

**Навіщо це потрібно:** Доступність (accessibility) та SEO.

**Простими словами:** Кожне зображення має мати опис, навіть якщо він порожній (для декоративних зображень).

---

### ✅ Перевірка 10: interactive aria-label attributes are not empty

**Що перевіряє:** Чи інтерактивні елементи мають не порожній `aria-label`?

**Що таке `aria-label`?** Це атрибут, який описує елемент для сліпих людей. Наприклад, кнопка "Закрити" може мати `aria-label="Закрити модальне вікно"`.

**Що таке інтерактивні елементи?** Це елементи, з якими можна взаємодіяти:
- Кнопки (`<button>`)
- Посилання (`<a>`)
- Поля вводу (`<input>`)
- Елементи з `role="button"` або `role="link"`

**Як працює:**
1. Знаходить всі інтерактивні елементи з `aria-label`
2. Перевіряє, чи `aria-label` не порожній
3. Якщо хоча б один елемент має порожній `aria-label`, тест падає

**Навіщо це потрібно:** Доступність. Якщо елемент має `aria-label`, він має бути корисним, а не порожнім.

**Простими словами:** Якщо кнопка має `aria-label=""`, це погано. Має бути `aria-label="Закрити"` або щось подібне.

---

### ✅ Перевірка 11: all links are HTTPS (no http:// hrefs)

**Що перевіряє:** Чи всі посилання використовують безпечний HTTPS, а не небезпечний HTTP?

**Як працює:**
1. Знаходить всі теги `<a>` з атрибутом `href`
2. Перевіряє кожне посилання:
   - Якщо посилання починається з `http://` (небезпечне), тест падає
   - Якщо посилання починається з `https://` (безпечне) або відносне (без протоколу), тест проходить
3. Ігнорує спеціальні посилання (`mailto:`, `tel:`, `#`, `javascript:`)

**Навіщо це потрібно:** Безпека. Всі посилання мають бути через HTTPS.

**Простими словами:** Всі посилання мають бути `https://...`, а не `http://...`.

---

### ✅ Перевірка 12: title exists and is not empty

**Що перевіряє:** Чи є заголовок сторінки (`<title>`) і чи він не порожній?

**Як працює:**
1. Знаходить тег `<title>` в HTML
2. Перевіряє, чи він існує
3. Перевіряє, чи він не порожній

**Навіщо це потрібно:** 
- SEO (заголовок показується в результатах пошуку)
- Користувачі бачать його на вкладці браузера

**Простими словами:** Сторінка має мати заголовок, який видно на вкладці браузера.

---

### ✅ Перевірка 13: has exactly one H1 and it matches expected; H1 is on first screen and first among headings

**Що перевіряє:** Чи є рівно один H1, чи він правильний, і чи він на першому екрані?

**Що таке H1?** Це найголовніший заголовок на сторінці. Наприклад, "AI Automator".

**Як працює:**
1. Знаходить всі теги `<h1>` на сторінці
2. Перевіряє, чи є рівно один H1 (не більше, не менше)
3. Перевіряє, чи H1 видимий
4. Перевіряє, чи H1 містить очікуваний текст (наприклад, "AI Automator")
5. Перевіряє, чи H1 знаходиться на першому екрані (в межах висоти екрана)
6. Перевіряє, чи H1 є першим заголовком в основному контенті (не в header/nav)

**Навіщо це потрібно:** 
- SEO (пошукові системи використовують H1 для розуміння теми сторінки)
- Структура (має бути один головний заголовок)

**Простими словами:** На сторінці має бути рівно один головний заголовок, він має бути видимий одразу, і він має бути правильним.

---

### ✅ Перевірка 14: has H2/H3 headings

**Що перевіряє:** Чи є на сторінці підзаголовки (H2 або H3)?

**Як працює:**
1. Рахує кількість тегів `<h2>` та `<h3>` на сторінці
2. Перевіряє, чи є хоча б один H2 або H3

**Навіщо це потрібно:** 
- Структура (сторінка має бути структурована)
- SEO (підзаголовки допомагають пошуковим системам зрозуміти структуру)

**Простими словами:** Сторінка має мати підзаголовки, не тільки один H1.

---

### ✅ Перевірка 15: has main CTA button "Записатися на курс" in hero

**Що перевіряє:** Чи є головна кнопка "Записатися на курс" в Hero секції?

**Що таке Hero секція?** Це верхня частина сторінки, яка зазвичай містить головний заголовок і кнопку.

**Як працює:**
1. Знаходить секцію з класом `hero`
2. Шукає в ній елемент з атрибутом `data-modal-open` (це кнопка, яка відкриває модалку)
3. Перевіряє, чи цей елемент містить текст "Записатися на курс" (або локалізовану версію)
4. Перевіряє, чи кнопка видима

**Навіщо це потрібно:** Це головна кнопка на сторінці, яка має бути видима і працювати.

**Простими словами:** В верхній частині сторінки має бути кнопка "Записатися на курс".

---

### ✅ Перевірка 16: has required sections/anchors per page (if configured)

**Що перевіряє:** Чи є на сторінці всі необхідні секції (якщо вони налаштовані)?

**Як працює:**
1. Перевіряє, чи в конфігурації (`pages.json`) вказані якорі (anchors) для цієї сторінки
2. Якщо вказані, перевіряє, чи кожен якір існує на сторінці
3. Також перевіряє, чи є секція зі знижкою (якщо налаштована)

**Навіщо це потрібно:** Переконатися, що на сторінці є всі необхідні секції (Benefits, About, Program, Format, Reviews).

**Простими словами:** Якщо в конфігурації вказано, що на сторінці має бути секція "#benefits", тест перевіряє, чи вона дійсно є.

---

## 📋 Тест-кейс #2: Header / Menu / Navigation

**Що це означає?** Перевірки верхньої частини сайту (header), меню та навігації.

### ✅ Перевірка 1: should navigate to homepage when clicking logo

**Що перевіряє:** Чи переходить користувач на головну сторінку, коли клікає на логотип?

**Як працює:**
1. Знаходить логотип в header (не в банері!)
2. Клікає на нього
3. Перевіряє, чи адреса змінилась на головну сторінку (наприклад, `/ua/` або `/ua-ru/`)
4. Перевіряє, чи адреса не містить `/courses/` (тобто ми вийшли зі сторінки курсу)

**Навіщо це потрібно:** Стандартна поведінка - логотип має вести на головну сторінку.

**Простими словами:** Клік на логотип має повернути на головну сторінку.

---

### ✅ Перевірка 2: should open/close menu on desktop and mobile

**Що перевіряє:** Чи відкривається/закривається меню на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375x667 пікселів)
2. Шукає кнопку "гамбургер" (три лінії, які відкривають меню)
3. Клікає на неї
4. Перевіряє, чи меню відкрилось (стало видимим)

**Навіщо це потрібно:** На мобільних пристроях меню зазвичай приховане і відкривається по кліку.

**Простими словами:** На телефоні має бути кнопка, яка відкриває меню.

---

### ✅ Перевірка 3: should open/close dropdown "курси/напрямки"

**Що перевіряє:** Чи працює випадаюче меню "Курси" на desktop?

**Як працює:**
1. Встановлює розмір екрана як на комп'ютері (1280x720)
2. Знаходить перший пункт меню (який містить "Курси")
3. Наводить мишку на нього (hover)
4. Перевіряє, чи випадаюче меню відкрилось (має класи `visible`, `opacity-100`)
5. Відводить мишку
6. Перевіряє, чи випадаюче меню закрилось (має класи `invisible`, `opacity-0`)

**Навіщо це потрібно:** На desktop випадаюче меню має відкриватись при наведенні мишки.

**Простими словами:** Коли наводиш мишку на "Курси", має з'явитись випадаюче меню зі списком курсів.

---

### ✅ Перевірка 4: should open/close courses dropdown on mobile (click toggle)

**Що перевіряє:** Чи працює випадаюче меню "Курси" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375x667)
2. Відкриває мобільне меню (клікає на "гамбургер")
3. Клікає на перший пункт меню (який містить "Курси")
4. Перевіряє, чи випадаюче меню відкрилось (має клас `mobile-open`)
5. Перевіряє, чи ми не перейшли на іншу сторінку (залишились на тій же адресі)
6. Клікає знову
7. Перевіряє, чи випадаюче меню закрилось

**Навіщо це потрібно:** На мобільних пристроях випадаюче меню відкривається по кліку, а не при наведенні.

**Простими словами:** На телефоні, коли клікаєш на "Курси", має з'явитись список курсів. Клік знову - закриється.

---

### ✅ Перевірка 5: should verify menu links (at least "Всі AI курси", "QA + AI", "Fullstack + AI")

**Що перевіряє:** Чи є в меню потрібні посилання?

**Як працює:**
1. Перевіряє, чи на сторінці є текст "Всі AI курси"
2. Якщо є, перевіряє, чи є посилання з цим текстом
3. Те саме для "QA + AI" та "Fullstack + AI"

**Навіщо це потрібно:** Переконатися, що в меню є важливі посилання.

**Простими словами:** В меню мають бути посилання "Всі AI курси", "QA + AI", "Fullstack + AI".

---

### ✅ Перевірка 6: should open "Обрати професію" in new tab and trigger dataLayer event

**Що перевіряє:** Чи кнопка "Обрати професію" відкривається в новій вкладці і чи відправляє подію в dataLayer?

**Що таке dataLayer?** Це спеціальний об'єкт, який використовується для аналітики (Google Analytics, тощо).

**Як працює:**
1. Знаходить кнопку "Обрати професію" (на desktop або mobile)
2. Перевіряє, чи вона має атрибут `target="_blank"` (відкривається в новій вкладці)
3. Клікає на неї
4. Перевіряє, чи в `dataLayer` з'явилась подія `register_click`

**Навіщо це потрібно:** 
- Кнопка має відкриватись в новій вкладці (щоб не втратити поточну сторінку)
- Подія в dataLayer потрібна для аналітики (щоб знати, скільки людей клікнули)

**Простими словами:** Кнопка "Обрати професію" має відкриватись в новій вкладці і відправляти подію для аналітики.

---

## 📋 Тест-кейс #3: Language switcher / Localization

**Що це означає?** Перевірки перемикача мови та локалізації (українська/російська).

### ✅ Перевірка 1: language switcher exists in header and is visible

**Що перевіряє:** Чи є перемикач мови в header і чи він видимий?

**Як працює:**
1. Знаходить header
2. Шукає в ньому елементи, які можуть бути перемикачем мови:
   - Посилання з `/ua/` або `/ua-ru/` в адресі
   - Кнопки з атрибутами `data-lang` або `data-locale`
   - Кнопки з текстом про мову
3. Перевіряє, чи знайшов хоча б один видимий елемент

**Навіщо це потрібно:** Користувач має мати можливість змінити мову.

**Простими словами:** В header має бути видимий перемикач мови.

---

### ✅ Перевірка 2: can navigate to alternate locale and URL changes correctly

**Що перевіряє:** Чи можна перейти на альтернативну мову і чи правильно змінюється URL?

**Як працює:**
1. Визначає поточну мову (наприклад, `ua` - українська)
2. Знаходить альтернативну мову (`ua-ru` - російська)
3. Створює URL для альтернативної мови
4. Переходить на цей URL
5. Перевіряє:
   - URL містить правильну мову (`/ua-ru/`)
   - URL точно такий, як очікувалось
   - Сторінка завантажилась (є `<body>` і `<h1>`)

**Навіщо це потрібно:** Перевіряє, що альтернативна мова взагалі існує і доступна.

**Простими словами:** Якщо ти на українській сторінці, маєш мати можливість перейти на російську версію.

---

### ✅ Перевірка 3: page content updates after locale change (H1 remains visible)

**Що перевіряє:** Чи оновлюється контент після зміни мови, і чи залишається H1 видимим?

**Як працює:**
1. Запам'ятовує текст H1 на поточній мові
2. Переходить на альтернативну мову
3. Перевіряє, чи H1 все ще є і видимий (текст може змінитись, але елемент має бути)

**Навіщо це потрібно:** Перевіряє, що структура сторінки не зламалась після зміни мови.

**Простими словами:** Після зміни мови заголовок має залишитись, навіть якщо текст змінився.

---

### ✅ Перевірка 4: alternate locale page works on mobile viewport

**Що перевіряє:** Чи працює альтернативна мова на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375x667 пікселів)
2. Переходить на альтернативну мову
3. Перевіряє, чи сторінка завантажилась і H1 видимий

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що перемикач мови працює і на мобільних.

**Простими словами:** На телефоні також має працювати перемикач мови.

---

### ✅ Перевірка 5: locale change preserves course page path (only locale prefix changes)

**Що перевіряє:** Чи зберігається правильна структура URL після зміни мови?

**Як працює:**
1. Переходить на альтернативну мову
2. Перевіряє, що:
   - Змінився тільки префікс мови (`/ua/` → `/ua-ru/`)
   - Залишився шлях до курсу (`/courses/`)
   - Залишився ключ сторінки (`ai-marketing-specialist`)

**Навіщо це потрібно:** Якщо структура URL зламається, користувач може потрапити не на ту сторінку.

**Простими словами:** Після зміни мови має змінитись тільки мова в URL, а не вся адреса.

**Приклад:**
- Було: `/ua/courses/ai-marketing-specialist/`
- Стало: `/ua-ru/courses/ai-marketing-specialist/`
- ✅ Правильно: змінилась тільки мова
- ❌ Неправильно: `/ua-ru/courses/` (втратився ключ сторінки)

---

### ✅ Перевірка 6: can navigate back to original locale (round-trip)

**Що перевіряє:** Чи можна повернутися назад на оригінальну мову?

**Як працює:**
1. Переходить на альтернативну мову
2. Перевіряє, чи URL правильний
3. Повертається на оригінальну мову
4. Перевіряє, чи повернувся на правильну адресу і H1 видимий

**Навіщо це потрібно:** Користувач має мати можливість повернутися назад, якщо помилився.

**Простими словами:** Ти маєш мати можливість перейти з української на російську і назад.

---

**Примітка:** Спочатку був тест відправки форми на ру локалі, але його прибрали, оскільки відправка форми - це частина тест-кейсу #5 (Modal form), який автоматично працює для обох локалей через `pages.json`.

---

## 📋 Тест-кейс #4: Hero block

**Що це означає?** Перевірки Hero секції (верхня частина сторінки з головним заголовком і кнопкою).

### ✅ Перевірка 1: hero section exists, is visible, and is on first screen

**Що перевіряє:** Чи є Hero секція, чи вона видима, і чи вона на першому екрані?

**Як працює:**
1. Знаходить секцію з класом `hero`
2. Перевіряє, чи вона існує
3. Перевіряє, чи вона видима
4. Перевіряє, чи вона знаходиться на першому екрані (в межах висоти екрана)

**Навіщо це потрібно:** Hero секція - це перше, що бачить користувач. Вона має бути видима одразу.

**Простими словами:** Верхня частина сторінки (Hero) має бути видима одразу, без прокрутки.

---

### ✅ Перевірка 2: hero contains the H1 with expected text

**Що перевіряє:** Чи містить Hero секція H1 з очікуваним текстом?

**Як працює:**
1. Знаходить Hero секцію
2. Шукає в ній тег `<h1>`
3. Перевіряє, чи є рівно один H1
4. Перевіряє, чи H1 видимий
5. Перевіряє, чи H1 містить очікуваний текст (наприклад, "AI Automator")

**Навіщо це потрібно:** H1 має бути в Hero секції, і він має бути правильним.

**Простими словами:** В Hero секції має бути один головний заголовок з правильним текстом.

---

### ✅ Перевірка 3: hero has primary CTA button and it is clickable

**Що перевіряє:** Чи є головна CTA кнопка в Hero і чи вона клікабельна?

**Що таке CTA?** Call To Action - кнопка, яка закликає до дії (наприклад, "Записатися на курс").

**Як працює:**
1. Знаходить Hero секцію
2. Шукає в ній кнопку з атрибутом `data-modal-open`
3. Перевіряє, чи кнопка містить правильний текст (локалізований)
4. Перевіряє, чи кнопка видима
5. Клікає на неї
6. Перевіряє, чи модалка відкрилась
7. Закриває модалку

**Навіщо це потрібно:** Головна кнопка має працювати і відкривати модалку.

**Простими словами:** В Hero секції має бути кнопка "Записатися на курс", яка відкриває форму.

---

### ✅ Перевірка 4: hero has supporting text (not only the H1)

**Що перевіряє:** Чи є в Hero секції додатковий текст (не тільки H1)?

**Як працює:**
1. Знаходить Hero секцію
2. Бере весь текст з неї
3. Перевіряє, чи довжина тексту більше 30 символів

**Навіщо це потрібно:** Hero секція має містити не тільки заголовок, але й опис.

**Простими словами:** В Hero секції має бути не тільки заголовок, але й якийсь опис.

---

## 📋 Тест-кейс #5: Modal "Записатися на курс" (форма)

**Що це означає?** Перевірки модальної форми "Записатися на курс".

### ✅ Перевірка 1: opens from hero and closes via close (X) button

**Що перевіряє:** Чи відкривається модалка з Hero і чи закривається кнопкою X?

**Як працює:**
1. Відкриває модалку з Hero секції
2. Перевіряє, чи модалка відкрилась (backdrop видимий, modal видимий, заголовок видимий)
3. Клікає на кнопку закриття (X)
4. Перевіряє, чи модалка закрилась (backdrop прихований, modal не видимий)

**Навіщо це потрібно:** Модалка має відкриватись і закриватись правильно.

**Простими словами:** Кнопка "Записатися на курс" має відкрити форму, а кнопка X - закрити її.

---

### ✅ Перевірка 2: opens from #about and closes via overlay click

**Що перевіряє:** Чи відкривається модалка з секції About і чи закривається кліком на overlay?

**Що таке overlay?** Це затемнений фон за модалкою. Клік на нього зазвичай закриває модалку.

**Як працює:**
1. Знаходить секцію `#about`
2. Знаходить в ній кнопку, яка відкриває модалку
3. Клікає на неї
4. Перевіряє, чи модалка відкрилась
5. Клікає на overlay (затемнений фон)
6. Перевіряє, чи модалка закрилась

**Навіщо це потрібно:** Модалка може відкриватись з різних місць, і має закриватись кліком на overlay.

**Простими словами:** Форму можна відкрити з секції "Про курс", і вона має закриватись кліком на затемнений фон.

---

### ✅ Перевірка 3: closes via Esc

**Що перевіряє:** Чи закривається модалка при натисканні клавіші Esc?

**Як працює:**
1. Відкриває модалку
2. Перевіряє, чи модалка відкрилась
3. Натискає клавішу Esc
4. Перевіряє, чи модалка закрилась

**Навіщо це потрібно:** Стандартна поведінка - Esc закриває модалку.

**Простими словами:** Натискання Esc має закривати форму.

---

### ✅ Перевірка 4: has required form fields and constraints (basic HTML attrs)

**Що перевіряє:** Чи є всі необхідні поля форми і чи вони мають правильні атрибути?

**Як працює:**
1. Відкриває модалку
2. Перевіряє форму:
   - Чи має атрибут `action` (куди відправляється форма)
   - Чи `action` містить правильний URL
3. Перевіряє поле "Ім'я" (`#name-input`):
   - Чи має атрибут `required` (обов'язкове)
   - Чи має `minlength="2"` (мінімум 2 символи)
   - Чи має `maxlength="30"` (максимум 30 символів)
4. Перевіряє поле "Телефон" (`#phone-input`):
   - Чи має `required`
   - Чи має `type="tel"` (тип - телефон)
   - Чи має `pattern` (патерн для валідації)
5. Перевіряє поле "Email" (`#email-input`):
   - Чи має `required`
   - Чи має `type="email"` (тип - email)
   - Чи має `maxlength="63"` (максимум 63 символи)
6. Перевіряє чекбокс "Політика конфіденційності" (`#user-policy`):
   - Чи має `required`
   - Чи має `type="checkbox"`
7. Перевіряє кнопку "Відправити":
   - Чи вона видима
   - Чи має правильний текст (локалізований)

**Навіщо це потрібно:** Всі поля мають мати правильні атрибути для валідації.

**Простими словами:** Форма має мати всі необхідні поля з правильними налаштуваннями (обов'язковість, мінімум/максимум символів).

---

### ✅ Перевірка 5: validation rules: Name field accepts required alphabets/symbols/spaces and max length 30

**Що перевіряє:** Чи поле "Ім'я" приймає правильні символи і чи обмежує довжину до 30?

**Як працює:**
1. Відкриває модалку
2. Очищає форму
3. Перевіряє різні варіанти:
   - **Латиниця:** Вводить "John Doe" → перевіряє, чи поле валідне
   - **Кирилиця:** Вводить "Марія" → перевіряє, чи поле валідне
   - **Спеціальні символи:** Вводить "ЯяЇїІіЄєҐґ'ʼ`- Марія" → перевіряє, чи поле валідне
   - **Пробіли:** Вводить "Марія " (з пробілом в кінці) → перевіряє, чи поле валідне
   - **Максимальна довжина:** Вводить 31 символ → перевіряє, чи поле обмежилось до 30 символів

**Навіщо це потрібно:** Поле має приймати правильні символи і не дозволяти занадто довгі імена.

**Простими словами:** Поле "Ім'я" має приймати українські/англійські букви, спеціальні символи, пробіли, але не більше 30 символів.

---

### ✅ Перевірка 6: validation rules: Phone accepts only digits (and optional +); letters are rejected

**Що перевіряє:** Чи поле "Телефон" приймає тільки цифри (і опціонально +), а літери відкидає?

**Як працює:**
1. Відкриває модалку
2. Очищає форму
3. Перевіряє:
   - **Тільки цифри:** Вводить "380635080808" → перевіряє, чи поле містить тільки цифри (і опціонально +)
   - **Літери:** Вводить "123abc" → перевіряє, чи поле стало невалідним (якщо літери залишились) або чи літери видалились

**Навіщо це потрібно:** Телефон має містити тільки цифри, не літери.

**Простими словами:** Поле "Телефон" має приймати тільки цифри (і можливо + на початку), але не літери.

---

### ✅ Перевірка 7: validation rules: Email format and allowed characters

**Що перевіряє:** Чи поле "Email" приймає правильний формат email і чи дозволені правильні символи?

**Як працює:**
1. Відкриває модалку
2. Очищає форму
3. Перевіряє різні варіанти:
   - **Мінімум 1 символ перед @:** Вводить "@example.com" → перевіряє, чи поле невалідне
   - **Дозволені символи:** Вводить "User.Name+tag_1-2@example.com" → перевіряє, чи поле валідне
   - **Дефіс не на початку/кінці:** Вводить "-user@example.com" → перевіряє, чи це заблоковано (якщо увімкнено STRICT_VALIDATION)
   - **Крапка не на початку/кінці:** Вводить ".user@example.com" → перевіряє, чи це заблоковано (якщо увімкнено STRICT_VALIDATION)
   - **Максимальна довжина:** Вводить email з 65 символами → перевіряє, чи поле обмежилось до 63 символів
   - **Великі літери:** Вводить "TEST.USER+TAG@EXAMPLE.COM" → перевіряє, чи поле валідне

**Навіщо це потрібно:** Email має мати правильний формат і не дозволяти неправильні символи.

**Простими словами:** Поле "Email" має приймати правильний формат email (наприклад, `user@example.com`), дозволяти латинські літери, цифри, дефіс, підкреслення, крапку, плюс, але не більше 63 символів.

---

### ✅ Перевірка 8: submits form (REAL lead, opt-in) and shows thank-you; verifies Zoho product mapping

**Що перевіряє:** Чи відправляється форма з реальними даними, чи показується "Дякуємо", і чи правильний Zoho product mapping?

**Що таке Zoho product mapping?** Це налаштування, яке каже системі, який продукт (курс) обрав користувач.

**Як працює:**
1. **ВАЖЛИВО:** Цей тест працює тільки якщо встановлено `REAL_SUBMIT=true` (інакше він пропускається)
2. Відкриває модалку
3. Перевіряє приховані поля форми:
   - `fields[product_name]` має значення з конфігурації (наприклад, "AI_Marketing_Automation_Global_From_Landing_Page")
   - `fields[product_id]` має значення з конфігурації (наприклад, "1819773001945816767")
   - `form_name` має значення з конфігурації (наприклад, "page-modal-form")
   - `.event-data` має атрибут `data-eventvalue` з конфігурації (наприклад, "CourseLead")
4. Заповнює форму тестовими даними
5. Перевіряє, чи форма валідна
6. Відправляє форму (РЕАЛЬНО, не stub!)
7. Перехоплює запит і перевіряє, чи в ньому правильні значення:
   - `fields[product_name]` правильний
   - `fields[product_id]` правильний
   - `form_name` правильний
   - `action` = "zoho_connector_form"
8. Перевіряє, чи з'явилось повідомлення "Дякуємо" (або редирект на thank-you сторінку)

**Навіщо це потрібно:** Перевіряє, що форма дійсно відправляється і що правильний продукт з'єднується з лідом.

**Простими словами:** Коли користувач заповнює форму, система має знати, який саме курс він обрав, і має показати "Дякуємо".

**⚠️ УВАГА:** Цей тест створює РЕАЛЬНИЙ лід в системі! Він працює тільки якщо явно увімкнено через `REAL_SUBMIT=true`.

---

## 📋 Тест-кейс #6: Benefits section (#benefits)

**Що це означає?** Перевірки секції "Переваги" (Benefits) - це частина сторінки, яка показує переваги курсу.

### ✅ Перевірка 1: benefits section exists and is visible

**Що перевіряє:** Чи є секція "Переваги" на сторінці і чи вона видима?

**Як працює:**
1. Знаходить елемент з `id="benefits"` (це секція переваг)
2. Перевіряє, чи він існує
3. Перевіряє, чи він видимий (не прихований)

**Навіщо це потрібно:** Секція переваг - це важлива частина сторінки курсу. Вона має бути видима.

**Простими словами:** На сторінці має бути секція "Переваги", і вона має бути видима.

---

### ✅ Перевірка 2: benefits section has a heading (H2 or H3)

**Що перевіряє:** Чи є в секції "Переваги" заголовок (H2 або H3)?

**Як працює:**
1. Знаходить секцію `#benefits`
2. Шукає в ній теги `<h2>` або `<h3>`
3. Перевіряє, чи є хоча б один видимий заголовок
4. Перевіряє, чи заголовок не порожній (має хоча б 3 символи)

**Навіщо це потрібно:** 
- Структура (секція має мати заголовок)
- SEO (заголовки допомагають пошуковим системам зрозуміти структуру)

**Простими словами:** Секція "Переваги" має мати заголовок, наприклад "Переваги курсу".

---

### ✅ Перевірка 3: benefits section contains benefit items (minimum 3)

**Що перевіряє:** Чи є в секції "Переваги" мінімум 3 елементи переваг?

**Як працює:**
1. Знаходить секцію `#benefits`
2. Шукає елементи переваг, використовуючи різні селектори:
   - `.benefit-card` (картки переваг)
   - `.benefit-item` (елементи переваг)
   - `[class*="benefit"]` (будь-які елементи з "benefit" в класі)
   - `article` (статті)
   - `li` (елементи списку)
   - `.card` (картки)
3. Якщо не знайдено, шукає прямі дочірні елементи секції
4. Перевіряє, чи знайдено мінімум 3 видимих елементи

**Навіщо це потрібно:** Секція переваг має містити кілька переваг, щоб бути корисною.

**Простими словами:** В секції "Переваги" має бути хоча б 3 переваги (наприклад, "Швидке навчання", "Практика", "Сертифікат").

---

### ✅ Перевірка 4: each benefit item has a title/heading

**Що перевіряє:** Чи має кожен елемент переваги заголовок?

**Як працює:**
1. Знаходить всі елементи переваг в секції `#benefits`
2. Для кожного елемента перевіряє, чи є в ньому:
   - Заголовок (`<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>`)
   - Або елемент з класом, що містить "title" або "heading"
   - Або жирний текст (`<strong>`, `<b>`)
3. Якщо хоча б один елемент не має заголовка, тест падає

**Навіщо це потрібно:** Кожна перевага має мати назву, щоб користувач міг швидко зрозуміти, що це за перевага.

**Простими словами:** Кожна перевага має мати назву, наприклад "Швидке навчання" або "Практика з реальними проектами".

---

### ✅ Перевірка 5: each benefit item has a description

**Що перевіряє:** Чи має кожен елемент переваги опис?

**Як працює:**
1. Знаходить всі елементи переваг в секції `#benefits`
2. Для кожного елемента:
   - Бере весь текст елемента
   - Видаляє текст заголовка
   - Залишається тільки опис
   - Перевіряє, чи опис має мінімум 10 символів
3. Якщо хоча б один елемент не має опису (або опис занадто короткий), тест падає

**Навіщо це потрібно:** Кожна перевага має мати опис, щоб користувач міг зрозуміти, що саме означає ця перевага.

**Простими словами:** Кожна перевага має мати не тільки назву, але й опис (хоча б 10 символів), наприклад "Навчайтесь в зручному для вас темпі".

---

### ✅ Перевірка 6: benefits section works on mobile viewport

**Що перевіряє:** Чи працює секція "Переваги" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась до мобільного розміру
3. Знаходить секцію `#benefits`
4. Перевіряє, чи вона видима
5. Перевіряє, чи вона має ширину і висоту (не нульові розміри)
6. Перевіряє, чи мінімум 3 елементи переваг видимі на мобільному

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що секція переваг правильно відображається на мобільних.

**Простими словами:** На телефоні секція "Переваги" також має бути видима і показувати мінімум 3 переваги.

---

## 📋 Тест-кейс #7: About section (#about)

**Що це означає?** Перевірки секції "Про курс" (About) - це частина сторінки, яка описує курс детальніше.

### ✅ Перевірка 1: about section exists and is visible

**Що перевіряє:** Чи є секція "Про курс" на сторінці і чи вона видима?

**Як працює:**
1. Знаходить елемент з `id="about"` (це секція "Про курс")
2. Перевіряє, чи він існує
3. Перевіряє, чи він видимий (не прихований через CSS)

**Навіщо це потрібно:** Секція "Про курс" - це важлива частина сторінки курсу. Вона має бути видима.

**Простими словами:** На сторінці має бути секція "Про курс", і вона має бути видима.

---

### ✅ Перевірка 2: about section has a heading (H2 or H3)

**Що перевіряє:** Чи є в секції "Про курс" заголовок (H2 або H3)?

**Як працює:**
1. Знаходить секцію `#about`
2. Шукає в ній теги `<h2>` або `<h3>`
3. Перевіряє, чи є хоча б один видимий заголовок
4. Перевіряє, чи заголовок не порожній (має хоча б 3 символи)

**Навіщо це потрібно:** 
- Структура (секція має мати заголовок)
- SEO (заголовки допомагають пошуковим системам зрозуміти структуру)

**Простими словами:** Секція "Про курс" має мати заголовок, наприклад "Про курс" або "Що ви дізнаєтесь".

---

### ✅ Перевірка 3: about section has content (text, images, or other elements)

**Що перевіряє:** Чи є в секції "Про курс" контент (текст, зображення, параграфи тощо)?

**Як працює:**
1. Знаходить секцію `#about`
2. Бере весь текст з неї
3. Перевіряє, чи текст має мінімум 50 символів
4. Перевіряє, чи є структурні елементи:
   - Параграфи (`<p>`)
   - Контейнери (`<div>`)
   - Списки (`<ul>`, `<ol>`)
   - Статті (`<article>`)
   - Зображення (`<img>`, `<picture>`)
   - Відео (`<video>`)

**Навіщо це потрібно:** Секція "Про курс" має містити корисний контент, не тільки заголовок.

**Простими словами:** В секції "Про курс" має бути текст (не менше 50 символів) і структурні елементи (параграфи, зображення тощо).

---

### ✅ Перевірка 4: about section has CTA button if present (opens modal)

**Що перевіряє:** Чи є CTA кнопка в секції "Про курс" і чи вона відкриває модалку (якщо кнопка є)?

**Що таке CTA?** Call To Action - кнопка, яка закликає до дії (наприклад, "Записатися на курс").

**Як працює:**
1. Знаходить секцію `#about`
2. Шукає в ній кнопку з атрибутом `data-modal-open`
3. Якщо кнопка є:
   - Прокручує до центру екрана
   - Клікає на неї
   - Перевіряє, чи модалка відкрилась (backdrop видимий, modal видимий)
   - Закриває модалку кнопкою X
4. Якщо кнопки немає:
   - Логує повідомлення, що кнопка не знайдена
   - Тест не падає (це опціонально)

**Навіщо це потрібно:** Якщо в секції "Про курс" є кнопка "Записатися на курс", вона має працювати.

**Простими словами:** Якщо в секції "Про курс" є кнопка "Записатися на курс", перевіряє, що вона відкриває форму. Якщо кнопки немає - це теж нормально.

---

### ✅ Перевірка 5: about section works on mobile viewport

**Що перевіряє:** Чи працює секція "Про курс" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась до мобільного розміру
3. Знаходить секцію `#about`
4. Перевіряє, чи вона видима
5. Перевіряє, чи вона має ширину і висоту (не нульові розміри)
6. Перевіряє, чи текстовий контент залишається (не менше 50 символів)

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що секція "Про курс" правильно відображається на мобільних.

**Простими словами:** На телефоні секція "Про курс" також має бути видима і містити контент.

---

## 📋 Тест-кейс #8: Course program (#program)

**Що це означає?** Перевірки секції "Програма курсу" (Course program) - це частина сторінки, яка показує програму навчання, зазвичай у вигляді акордеону (розгортаючі елементи).

**Що таке акордеон?** Це елемент інтерфейсу, який можна розгорнути (відкрити) або згорнути (закрити), щоб показати або приховати контент.

### ✅ Перевірка 1: program section exists and is visible

**Що перевіряє:** Чи є секція "Програма курсу" на сторінці і чи вона видима?

**Як працює:**
1. Знаходить елемент з `id="program"` (це секція програми курсу)
2. Перевіряє, чи він існує
3. Перевіряє, чи він видимий (не прихований)

**Навіщо це потрібно:** Секція програми курсу - це важлива частина сторінки. Вона має бути видима.

**Простими словами:** На сторінці має бути секція "Програма курсу", і вона має бути видима.

---

### ✅ Перевірка 2: program section has a heading (H2 or H3)

**Що перевіряє:** Чи є в секції "Програма курсу" заголовок (H2 або H3)?

**Як працює:**
1. Знаходить секцію `#program`
2. Шукає в ній теги `<h2>` або `<h3>`
3. Перевіряє, чи є хоча б один видимий заголовок
4. Перевіряє, чи заголовок не порожній (має хоча б 3 символи)

**Навіщо це потрібно:** 
- Структура (секція має мати заголовок)
- SEO (заголовки допомагають пошуковим системам зрозуміти структуру)

**Простими словами:** Секція "Програма курсу" має мати заголовок, наприклад "Програма курсу" або "Що ви вивчите".

---

### ✅ Перевірка 3: program section contains accordion items (minimum 1)

**Що перевіряє:** Чи є в секції "Програма курсу" мінімум 1 елемент акордеону?

**Як працює:**
1. Знаходить секцію `#program`
2. Шукає елементи акордеону, використовуючи різні селектори:
   - Елементи з класами, що містять "accordion" або "item"
   - HTML5 елементи `<details>` (нативний акордеон)
   - Елементи з `aria-expanded` (стандарт для акордеонів)
3. Якщо не знайдено, шукає прямі дочірні елементи секції
4. Перевіряє, чи знайдено мінімум 1 видимий елемент

**Навіщо це потрібно:** Програма курсу зазвичай показується у вигляді акордеону, де кожен модуль можна розгорнути.

**Простими словами:** В секції "Програма курсу" має бути хоча б один елемент акордеону (наприклад, "Модуль 1", "Модуль 2").

---

### ✅ Перевірка 4: accordion items can be expanded and collapsed

**Що перевіряє:** Чи можна розгорнути і згорнути елементи акордеону?

**Як працює:**
1. Знаходить перший елемент акордеону в секції `#program`
2. Перевіряє тип акордеону:
   - **HTML5 `<details>`:** Перевіряє атрибут `open` і клікає на `<summary>`
   - **Кастомний акордеон:** Перевіряє атрибут `aria-expanded` і клікає на кнопку/тригер
3. Клікає на елемент, щоб розгорнути/згорнути
4. Чекає 300 мс (для анімації)
5. Перевіряє, чи стан змінився (відкрито/закрито)

**Навіщо це потрібно:** Акордеон має працювати - користувач має мати можливість розгорнути і згорнути модулі програми.

**Простими словами:** Коли клікаєш на модуль програми, він має розгортатись (показувати деталі) або згортатись (приховувати деталі).

---

### ✅ Перевірка 5: each accordion item has a title/heading

**Що перевіряє:** Чи має кожен елемент акордеону заголовок?

**Як працює:**
1. Знаходить всі елементи акордеону в секції `#program`
2. Для кожного елемента перевіряє, чи є заголовок:
   - Для HTML5 `<details>`: перевіряє наявність `<summary>`
   - Для кастомного акордеону: перевіряє наявність заголовка (H2-H6), класу з "title"/"heading", або тексту в кнопці
3. Рахує, скільки елементів мають заголовки
4. Перевіряє, чи більшість елементів (≥50%) мають заголовки

**Навіщо це потрібно:** Кожен модуль програми має мати назву, щоб користувач міг зрозуміти, що в ньому.

**Простими словами:** Більшість модулів програми (хоча б половина) мають мати назви, наприклад "Модуль 1: Основи", "Модуль 2: Поглиблене навчання".

---

### ✅ Перевірка 6: program section works on mobile viewport

**Що перевіряє:** Чи працює секція "Програма курсу" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась до мобільного розміру
3. Знаходить секцію `#program`
4. Перевіряє, чи вона видима
5. Перевіряє, чи вона має ширину і висоту (не нульові розміри)
6. Перевіряє, чи елементи акордеону видимі на мобільному

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що програма курсу правильно відображається на мобільних.

**Простими словами:** На телефоні секція "Програма курсу" також має бути видима і показувати елементи акордеону.

---

## 📋 Тест-кейс #9: Learning format (#format)

**Що це означає?** Перевірки секції "Формат навчання" (Learning format) - це частина сторінки, яка описує, як саме проходить навчання (онлайн, офлайн, вебінари, тривалість тощо).

### ✅ Перевірка 1: format section exists and is visible

**Що перевіряє:** Чи є секція "Формат навчання" на сторінці і чи вона видима?

**Як працює:**
1. Знаходить елемент з `id="format"` (це секція формату навчання)
2. Перевіряє, чи він існує
3. Перевіряє, чи він видимий (не прихований)

**Навіщо це потрібно:** Секція формату навчання - це важлива частина сторінки курсу. Вона має бути видима.

**Простими словами:** На сторінці має бути секція "Формат навчання", і вона має бути видима.

---

### ✅ Перевірка 2: format section has a heading (H2 or H3)

**Що перевіряє:** Чи є в секції "Формат навчання" заголовок (H2 або H3)?

**Як працює:**
1. Знаходить секцію `#format`
2. Шукає в ній теги `<h2>` або `<h3>`
3. Перевіряє, чи є хоча б один видимий заголовок
4. Перевіряє, чи заголовок не порожній (має хоча б 3 символи)

**Навіщо це потрібно:** 
- Структура (секція має мати заголовок)
- SEO (заголовки допомагають пошуковим системам зрозуміти структуру)

**Простими словами:** Секція "Формат навчання" має мати заголовок, наприклад "Формат навчання" або "Як проходить навчання".

---

### ✅ Перевірка 3: format section has content (text, images, or other elements)

**Що перевіряє:** Чи є в секції "Формат навчання" контент (текст, зображення, параграфи тощо)?

**Як працює:**
1. Знаходить секцію `#format`
2. Бере весь текст з неї
3. Перевіряє, чи текст має мінімум 50 символів
4. Перевіряє, чи є структурні елементи (параграфи, divs, списки, зображення, відео)

**Навіщо це потрібно:** Секція формату навчання має містити корисний контент, не тільки заголовок.

**Простими словами:** В секції "Формат навчання" має бути текст (не менше 50 символів) і структурні елементи (параграфи, зображення тощо).

---

### ✅ Перевірка 4: format section describes learning format details

**Що перевіряє:** Чи містить секція інформацію про формат навчання (ключові слова або елементи)?

**Як працює:**
1. Знаходить секцію `#format`
2. Бере весь текст з неї і перетворює на нижній регістр
3. Шукає ключові слова, пов'язані з форматом:
   - Українські: "формат", "навчання", "онлайн", "офлайн", "вебінар", "лекці", "практик", "час", "тривалість", "графік"
   - Російські: "обучение", "вебинар", "лекция", "практика", "время", "длительность", "расписание"
   - Англійські: "schedule", "duration"
4. Також шукає елементи з класами, що містять "format", "schedule", "duration"
5. Якщо не знайдено ключових слів, але секція має контент (≥50 символів), це теж OK

**Навіщо це потрібно:** Секція має містити інформацію про формат навчання, а не просто бути порожньою.

**Простими словами:** В секції "Формат навчання" має бути інформація про те, як проходить навчання (наприклад, "онлайн", "вебінари", "тривалість 3 місяці").

---

### ✅ Перевірка 5: format section works on mobile viewport

**Що перевіряє:** Чи працює секція "Формат навчання" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась до мобільного розміру
3. Знаходить секцію `#format`
4. Перевіряє, чи вона видима
5. Перевіряє, чи вона має ширину і висоту (не нульові розміри)
6. Перевіряє, чи текстовий контент залишається (не менше 50 символів)

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що секція формату навчання правильно відображається на мобільних.

**Простими словами:** На телефоні секція "Формат навчання" також має бути видима і містити контент.

---

## 📋 Тест-кейс #10: Reviews (#reviews)

**Що це означає?** Перевірки секції "Відгуки" (Reviews) - це частина сторінки, яка показує відгуки студентів про курс.

### ✅ Перевірка 1: reviews section exists and is visible

**Що перевіряє:** Чи є секція "Відгуки" на сторінці і чи вона видима?

**Як працює:**
1. Знаходить елемент з `id="reviews"` (це секція відгуків)
2. Перевіряє, чи він існує
3. Перевіряє, чи він видимий (не прихований)

**Навіщо це потрібно:** Секція відгуків - це важлива частина сторінки курсу. Вона має бути видима.

**Простими словами:** На сторінці має бути секція "Відгуки", і вона має бути видима.

---

### ✅ Перевірка 2: reviews section has a heading (H2 or H3) or title (optional)

**Що перевіряє:** Чи є в секції "Відгуки" заголовок (H2 або H3) або заголовок (опціонально)?

**Як працює:**
1. Знаходить секцію `#reviews`
2. Шукає в ній заголовки (H2-H6), елементи з класами "title"/"heading", або перевіряє, чи є текст (≥10 символів)
3. Якщо знайдено - логує, що заголовок є
4. Якщо не знайдено - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** Секція має мати заголовок, але якщо його немає, це не критично.

**Простими словами:** Секція "Відгуки" має мати заголовок, але якщо його немає - це не проблема.

---

### ✅ Перевірка 3: reviews section contains review items or has content (optional)

**Що перевіряє:** Чи є в секції "Відгуки" елементи відгуків або контент (опціонально)?

**Як працює:**
1. Знаходить секцію `#reviews`
2. Шукає елементи відгуків, використовуючи різні селектори:
   - Елементи з класами, що містять "review" або "testimonial"
   - Елементи з класами `.review-item`, `.testimonial-item`
   - Елементи `<article>`, `.card`
3. Якщо не знайдено, шукає прямі дочірні елементи секції
4. Перевіряє, чи є текст (≥20 символів) або зображення/відео
5. Якщо знайдено елементи або контент - логує
6. Якщо не знайдено - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** Секція відгуків може бути порожньою або мати іншу структуру, тому це опціонально.

**Простими словами:** В секції "Відгуки" можуть бути відгуки, але якщо їх немає - це не проблема.

---

### ✅ Перевірка 4: review items have content (text or rating) if present

**Що перевіряє:** Чи мають елементи відгуків контент (текст або рейтинг), якщо вони є?

**Як працює:**
1. Знаходить всі елементи відгуків в секції `#reviews`
2. Якщо елементів немає - логує і виходить (це OK)
3. Якщо елементи є, для кожного перевіряє:
   - Чи є текст (≥10 символів)
   - Чи є рейтинг (елементи з класами "rating", "star", або `aria-label` зі "star")
4. Якщо хоча б один елемент не має контенту - тест падає

**Навіщо це потрібно:** Якщо є відгуки, вони мають містити текст або рейтинг.

**Простими словами:** Якщо в секції "Відгуки" є відгуки, кожен має мати текст або зірки (рейтинг).

---

### ✅ Перевірка 5: reviews section works on mobile viewport

**Що перевіряє:** Чи працює секція "Відгуки" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась до мобільного розміру
3. Знаходить секцію `#reviews`
4. Перевіряє, чи вона видима
5. Перевіряє, чи вона має ширину і висоту (не нульові розміри)
6. Перевіряє, чи є контент (≥20 символів або зображення/відео) - опціонально

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що секція відгуків правильно відображається на мобільних.

**Простими словами:** На телефоні секція "Відгуки" також має бути видима.

---

## 📋 Тест-кейс #11: Pricing section

**Що це означає?** Перевірки секції "Ціна" (Pricing) - це частина сторінки, яка показує вартість курсу.

### ✅ Перевірка 1: pricing section exists and is visible

**Що перевіряє:** Чи є секція "Ціна" на сторінці і чи вона видима?

**Як працює:**
1. Шукає секцію ціни, використовуючи різні селектори:
   - `#pricing`
   - `#price`
   - Елементи з класами, що містять "pricing" або "price"
   - `section.discount.section` (з конфігурації `pages.json`)
2. Перевіряє, чи знайдена секція існує і видима

**Навіщо це потрібно:** Секція ціни - це важлива частина сторінки курсу. Вона має бути видима.

**Простими словами:** На сторінці має бути секція "Ціна", і вона має бути видима.

---

### ✅ Перевірка 2: pricing section has a heading or title

**Що перевіряє:** Чи є в секції "Ціна" заголовок або заголовок?

**Як працює:**
1. Знаходить секцію ціни
2. Шукає в ній заголовки (H2-H6), елементи з класами "title"/"heading", або перевіряє, чи є текст (≥10 символів)
3. Перевіряє, чи хоча б один з цих індикаторів присутній

**Навіщо це потрібно:** Секція має мати заголовок для структури та SEO.

**Простими словами:** Секція "Ціна" має мати заголовок, наприклад "Вартість курсу" або "Ціна".

---

### ✅ Перевірка 3: pricing section contains price information (if available)

**Що перевіряє:** Чи містить секція "Ціна" інформацію про ціну (якщо доступна)?

**Як працює:**
1. Знаходить секцію ціни
2. Бере весь текст з неї
3. Шукає індикатори ціни:
   - Числа з символами валют (₴, $, €, £, ₽) - наприклад, "1000₴"
   - Ключові слова: "цін", "price", "стоимость", "вартість", "цена"
   - Елементи з класами "price", "cost", "amount"
   - Елементи з атрибутами `data-price`, `data-cost`
4. Якщо знайдено - логує, що інформація про ціну є
5. Якщо не знайдено - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** Секція ціни має містити інформацію про вартість, але якщо її немає (наприклад, "Зв'яжіться з нами"), це теж OK.

**Простими словами:** В секції "Ціна" має бути інформація про вартість (наприклад, "1000₴"), але якщо її немає - це не проблема.

---

### ✅ Перевірка 4: pricing section has CTA button if present (opens modal)

**Що перевіряє:** Чи є CTA кнопка в секції "Ціна" і чи вона відкриває модалку (якщо кнопка є)?

**Що таке CTA?** Call To Action - кнопка, яка закликає до дії (наприклад, "Записатися на курс").

**Як працює:**
1. Знаходить секцію ціни
2. Шукає в ній кнопку з атрибутом `data-modal-open`
3. Якщо кнопка є:
   - Прокручує до центру екрана
   - Клікає на неї
   - Перевіряє, чи модалка відкрилась (backdrop видимий, modal видимий)
   - Закриває модалку кнопкою X
4. Якщо кнопки немає:
   - Логує повідомлення, що кнопка не знайдена
   - Тест не падає (це опціонально)

**Навіщо це потрібно:** Якщо в секції ціни є кнопка "Записатися на курс", вона має працювати.

**Простими словами:** Якщо в секції "Ціна" є кнопка "Записатися на курс", перевіряє, що вона відкриває форму. Якщо кнопки немає - це теж нормально.

---

### ✅ Перевірка 5: pricing section works on mobile viewport

**Що перевіряє:** Чи працює секція "Ціна" на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась до мобільного розміру
3. Знаходить секцію ціни
4. Перевіряє, чи вона видима
5. Перевіряє, чи вона має ширину і висоту (не нульові розміри)
6. Перевіряє, чи є інформація про ціну (опціонально)

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що секція ціни правильно відображається на мобільних.

**Простими словами:** На телефоні секція "Ціна" також має бути видима.

---

## 📋 Тест-кейс #12: Footer

**Що це означає?** Footer (підвал) - це нижня частина сторінки, яка зазвичай містить посилання, інформацію про компанію, copyright.

### ✅ Перевірка 1: footer exists and is visible

**Що перевіряє:** Чи є footer на сторінці і чи він видимий?

**Як працює:**
1. Шукає footer, використовуючи різні селектори:
   - `<footer>` (семантичний HTML тег)
   - `[role="contentinfo"]` (ARIA роль для footer)
   - Елементи з класами, що містять "footer"
   - `#footer` (ID)
2. Перевіряє, чи знайдений footer існує і видимий

**Навіщо це потрібно:** Footer - це важлива частина сторінки, яка містить корисну інформацію та посилання.

**Простими словами:** На сторінці має бути footer (підвал), і він має бути видимий.

---

### ✅ Перевірка 2: footer has content (text, links, or other elements)

**Що перевіряє:** Чи має footer контент (текст, посилання або інші елементи)?

**Як працює:**
1. Знаходить footer
2. Перевіряє, чи є текст (≥20 символів)
3. Перевіряє, чи є структурні елементи (посилання, параграфи, списки, зображення)

**Навіщо це потрібно:** Footer має містити корисну інформацію, а не бути порожнім.

**Простими словами:** Footer має містити текст, посилання або інші елементи, а не бути порожнім.

---

### ✅ Перевірка 3: footer contains links (minimum 1)

**Що перевіряє:** Чи містить footer хоча б одне посилання?

**Як працює:**
1. Знаходить footer
2. Шукає всі посилання (`<a>`) в footer
3. Перевіряє, чи є хоча б одне видиме посилання

**Навіщо це потрібно:** Footer зазвичай містить посилання на важливі сторінки (про нас, контакти, політика конфіденційності).

**Простими словами:** В footer має бути хоча б одне посилання (наприклад, "Про нас", "Контакти").

---

### ✅ Перевірка 4: footer links use HTTPS (no http:// hrefs)

**Що перевіряє:** Чи використовують посилання в footer HTTPS (безпечний протокол)?

**Як працює:**
1. Знаходить всі посилання в footer
2. Для кожного посилання перевіряє:
   - Якщо `href` починається з `http://` (небезпечний) - це помилка
   - Якщо `href` починається з `https://` (безпечний) - це OK
   - Якщо `href` відносний (без протоколу) - це OK (успадковує HTTPS)
   - Якщо `href` спеціальний (`mailto:`, `tel:`, `#`, `javascript:`) - це OK

**Навіщо це потрібно:** Всі посилання мають використовувати безпечний HTTPS протокол.

**Простими словами:** Всі посилання в footer мають використовувати HTTPS (безпечний), а не HTTP (небезпечний).

---

### ✅ Перевірка 5: footer has copyright or company information

**Що перевіряє:** Чи містить footer інформацію про copyright (авторські права) або компанію?

**Як працює:**
1. Знаходить footer
2. Бере весь текст з footer
3. Шукає індикатори copyright або інформації про компанію:
   - Символ ©
   - Ключові слова: "copyright", "авторське право", "авторское право"
   - "всі права", "все права", "all rights"
   - Назва компанії (наприклад, "goit")
   - Рік (наприклад, 2024, 2025)

**Навіщо це потрібно:** Footer зазвичай містить інформацію про copyright та компанію.

**Простими словами:** В footer має бути інформація про авторські права (наприклад, "© 2024 GoIT") або назва компанії.

---

### ✅ Перевірка 6: footer works on mobile viewport

**Що перевіряє:** Чи працює footer на мобільних пристроях?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась
3. Знаходить footer
4. Перевіряє, чи він видимий
5. Перевіряє, чи він має ширину і висоту (не нульові розміри)
6. Перевіряє, чи є текст (≥20 символів)
7. Перевіряє, чи є хоча б одне посилання

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що footer правильно відображається на мобільних.

**Простими словами:** На телефоні footer також має бути видимий і містити контент.

---

### ✅ Перевірка 7: footer form exists and is visible (if present)

**Що перевіряє:** Чи є форма в footer і чи вона видима (якщо форма є)?

**Як працює:**
1. Знаходить footer
2. Шукає форму в footer (використовуючи `#footerForm`, `[id*="footerForm"]`, або `form[data-form]`)
3. Якщо форма знайдена:
   - Перевіряє, чи вона видима
   - Перевіряє, чи є заголовок форми (H2)
4. Якщо форми немає - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** В footer може бути форма для замовлення консультації. Якщо вона є, вона має бути видима.

**Простими словами:** Якщо в footer є форма "Допоможемо з вибором", вона має бути видима.

---

### ✅ Перевірка 8: footer form has required fields (if form is present)

**Що перевіряє:** Чи має форма в footer обов'язкові поля (якщо форма є)?

**Як працює:**
1. Знаходить форму в footer
2. Якщо форма знайдена, перевіряє наявність полів:
   - Поле імені (`#name-input` або `input[name*="name"]`)
   - Поле телефону (`#phone-input` або `input[type="tel"]`)
   - Поле email (`#email-input` або `input[type="email"]`)
   - Чекбокс політики (`#user-policy` або `input[type="checkbox"][name*="policy"]`)
3. Якщо форми немає - логує і виходить (тест не падає)

**Навіщо це потрібно:** Форма має містити всі необхідні поля для замовлення консультації.

**Простими словами:** Якщо в footer є форма, вона має мати поля: ім'я, телефон, email та чекбокс згоди.

---

### ✅ Перевірка 9: footer form submit button exists and is clickable (if form is present)

**Що перевіряє:** Чи є кнопка відправки форми в footer і чи вона клікабельна (якщо форма є)?

**Як працює:**
1. Знаходить форму в footer
2. Якщо форма знайдена:
   - Шукає кнопку відправки (`button[type="submit"]`)
   - Перевіряє, чи вона видима
   - Перевіряє, чи вона не заблокована (`disabled`)
   - Перевіряє, чи вона має текст
3. Якщо форми немає - логує і виходить (тест не падає)

**Навіщо це потрібно:** Форма має мати кнопку для відправки, і вона має бути доступна для кліку.

**Простими словами:** Якщо в footer є форма, вона має мати кнопку "Замовити консультацію", яку можна натиснути.

---

### ✅ Перевірка 10: footer form works on mobile viewport (if form is present)

**Що перевіряє:** Чи працює форма в footer на мобільних пристроях (якщо форма є)?

**Як працює:**
1. Встановлює розмір екрана як на телефоні (375×667 пікселів)
2. Чекає 1 секунду, щоб сторінка адаптувалась
3. Знаходить форму в footer
4. Якщо форма знайдена:
   - Перевіряє, чи вона видима
   - Перевіряє, чи вона має ширину і висоту (не нульові розміри)
   - Перевіряє, чи поля форми видимі на mobile
5. Якщо форми немає - логує і виходить (тест не падає)

**Навіщо це потрібно:** Багато користувачів заходять з телефонів, тому важливо перевірити, що форма в footer правильно відображається на мобільних.

**Простими словами:** На телефоні форма в footer також має бути видима і працювати.

---

## 📋 Тест-кейс #13: Analytics / dataLayer

**Що це означає?** Analytics (аналітика) - це збір даних про те, як користувачі взаємодіють зі сайтом. dataLayer - це JavaScript масив, який зберігає ці дані для відправки в Google Tag Manager або інші системи аналітики.

### ✅ Перевірка 1: dataLayer exists and is initialized

**Що перевіряє:** Чи існує dataLayer і чи він ініціалізований?

**Як працює:**
1. Перевіряє, чи існує `window.dataLayer` (глобальна змінна в браузері)
2. Перевіряє, чи dataLayer є масивом (array)

**Навіщо це потрібно:** dataLayer - це основа для відстеження подій на сайті (кліки, відправка форм, перегляди сторінок).

**Простими словами:** На сторінці має бути dataLayer (спеціальна змінна для збору даних про дії користувачів).

---

### ✅ Перевірка 2: page view event is tracked (if implemented)

**Що перевіряє:** Чи відстежується подія перегляду сторінки (якщо реалізовано)?

**Як працює:**
1. Знаходить dataLayer
2. Шукає події перегляду сторінки:
   - `event: 'page_view'`
   - `event: 'pageview'`
   - `event: 'PageView'`
   - `eventType: 'page_view'`
   - Або ecommerce події (GTM)
3. Якщо знайдено - логує
4. Якщо не знайдено - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** Відстеження переглядів сторінок допомагає зрозуміти, які сторінки найпопулярніші.

**Простими словами:** Коли користувач відкриває сторінку, це має фіксуватись в dataLayer (якщо це реалізовано).

---

### ✅ Перевірка 3: CTA button click triggers dataLayer event (register_click)

**Що перевіряє:** Чи викликає клік на CTA кнопку подію в dataLayer (`register_click`)?

**Як працює:**
1. Знаходить CTA кнопку в hero секції (з атрибутом `data-modal-open`)
2. Очищає dataLayer (щоб ізолювати подію від попередніх)
3. Клікає на кнопку
4. Чекає 500 мілісекунд
5. Перевіряє, чи в dataLayer є подія `event: 'register_click'`
6. Закриває модалку

**Навіщо це потрібно:** Відстеження кліків на CTA кнопки допомагає зрозуміти, скільки користувачів намагаються записатись на курс.

**Простими словами:** Коли користувач клікає на кнопку "Записатися на курс", це має фіксуватись в dataLayer як подія `register_click`.

---

### ✅ Перевірка 4: form submission triggers dataLayer event (if implemented)

**Що перевіряє:** Чи викликає відправка форми подію в dataLayer (якщо реалізовано)?

**Як працює:**
1. Відкриває модалку форми (клікає на CTA кнопку)
2. Очищає dataLayer
3. Заповнює форму (ім'я, телефон, email, чекбокс)
4. Відправляє форму (stub запит, щоб не створювати реальний лід)
5. Чекає 1 секунду
6. Шукає події відправки форми:
   - `event: 'form_submit'`
   - `event: 'formSubmit'`
   - `event: 'lead_submit'`
   - `event: 'CourseLead'`
   - `eventType: 'form_submit'`
7. Якщо знайдено - логує
8. Якщо не знайдено - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** Відстеження відправки форм допомагає зрозуміти, скільки користувачів заповнили форму.

**Простими словами:** Коли користувач відправляє форму, це має фіксуватись в dataLayer (якщо це реалізовано).

---

### ✅ Перевірка 5: dataLayer events have valid structure (if events exist)

**Що перевіряє:** Чи мають події в dataLayer валідну структуру (якщо події є)?

**Як працює:**
1. Знаходить dataLayer
2. Якщо dataLayer порожній - логує і виходить (це OK)
3. Якщо dataLayer не порожній, для кожного елемента перевіряє:
   - Чи це об'єкт або масив (GTM може пушити масиви типу `['js', {...}]`)
   - Чи це не `null`
4. Логує, скільки елементів знайдено

**Навіщо це потрібно:** Події в dataLayer мають мати правильну структуру, щоб аналітичні системи могли їх обробити.

**Простими словами:** Всі події в dataLayer мають бути об'єктами або масивами (не порожніми або невалідними).

---

### ✅ Перевірка 6: Google Tag Manager container is loaded (if implemented)

**Що перевіряє:** Чи завантажений контейнер Google Tag Manager (якщо реалізовано)?

**Як працює:**
1. Перевіряє наявність GTM:
   - `window.google_tag_manager` (глобальна змінна GTM)
   - `window.dataLayer` (dataLayer зазвичай створюється GTM)
   - `window.gtag` (Google Analytics gtag)
   - Скрипти з `googletagmanager` в `<script>` тегах
2. Якщо знайдено - логує
3. Якщо не знайдено - логує, що це опціонально (тест не падає)

**Навіщо це потрібно:** GTM - це популярна система для управління аналітикою. Якщо вона використовується, вона має бути завантажена.

**Простими словами:** Якщо на сайті використовується Google Tag Manager, він має бути завантажений (якщо це реалізовано).

---

## 📋 Тест-кейс #14: Responsive / Cross-browser

**Що це означає?** Responsive (адаптивний) - це коли сайт правильно відображається на різних розмірах екранів (телефон, планшет, комп'ютер). Cross-browser - це коли сайт працює в різних браузерах (Chrome, Firefox, Safari).

### ✅ Перевірка 1: page loads correctly on mobile viewport (375x667)

**Що перевіряє:** Чи правильно завантажується сторінка на мобільному пристрої (розмір екрана як на iPhone SE)?

**Як працює:**
1. Встановлює розмір екрана 375×667 пікселів (як на маленькому телефоні)
2. Чекає 1 секунду, щоб сторінка адаптувалась
3. Перевіряє, чи завантажилась сторінка (є `<body>`, є заголовок)
4. Перевіряє, чи немає горизонтального скролу (ширина сторінки не більша за ширину екрана)

**Навіщо це потрібно:** Багато користувачів заходять з телефонів. Сторінка має правильно відображатись на мобільних.

**Простими словами:** На маленькому телефоні сторінка має завантажуватись і не має бути горизонтального скролу (щоб не довелось "ковзати" вліво-вправо).

---

### ✅ Перевірка 2: page loads correctly on tablet viewport (768x1024)

**Що перевіряє:** Чи правильно завантажується сторінка на планшеті (розмір екрана як на iPad)?

**Як працює:**
1. Встановлює розмір екрана 768×1024 пікселів (як на планшеті)
2. Чекає 1 секунду, щоб сторінка адаптувалась
3. Перевіряє, чи завантажилась сторінка (є `<body>`, є заголовок)
4. Перевіряє, чи немає горизонтального скролу

**Навіщо це потрібно:** Багато користувачів використовують планшети. Сторінка має правильно відображатись і на них.

**Простими словами:** На планшеті сторінка має завантажуватись і не має бути горизонтального скролу.

---

### ✅ Перевірка 3: page loads correctly on desktop viewport (1920x1080)

**Що перевіряє:** Чи правильно завантажується сторінка на великому екрані (розмір екрана як на комп'ютері Full HD)?

**Як працює:**
1. Встановлює розмір екрана 1920×1080 пікселів (як на великому моніторі)
2. Чекає 1 секунду, щоб сторінка адаптувалась
3. Перевіряє, чи завантажилась сторінка (є `<body>`, є заголовок)
4. Перевіряє, чи немає горизонтального скролу

**Навіщо це потрібно:** На великих екранах сторінка також має правильно відображатись.

**Простими словами:** На великому екрані сторінка має завантажуватись і не має бути горизонтального скролу.

---

### ✅ Перевірка 4: hero section is visible and properly sized on all viewports

**Що перевіряє:** Чи видима і правильно розміщена секція Hero (головна секція з заголовком) на всіх розмірах екранів?

**Як працює:**
1. Перевіряє на трьох розмірах екранів: mobile (375×667), tablet (768×1024), desktop (1920×1080)
2. Для кожного розміру:
   - Знаходить секцію Hero (використовуючи різні селектори: `#hero`, `section.hero`, елементи з класом "hero")
   - Перевіряє, чи вона видима
   - Перевіряє, чи вона має ширину і висоту (не нульові розміри)
   - Перевіряє, чи вона не виходить за межі екрана (ширина не більша за ширину екрана)

**Навіщо це потрібно:** Секція Hero - це перше, що бачить користувач. Вона має бути видима на всіх пристроях.

**Простими словами:** На телефоні, планшеті та комп'ютері секція Hero має бути видима і не виходити за межі екрана.

---

### ✅ Перевірка 5: main content is readable on all viewports (text is not too small)

**Що перевіряє:** Чи читабельний основний контент на всіх розмірах екранів (текст не занадто малий)?

**Як працює:**
1. Перевіряє на трьох розмірах екранів: mobile, tablet, desktop
2. Для кожного розміру:
   - Знаходить основний контент (`<main>` або `<body>`)
   - Перевіряє, чи є текст (≥50 символів)
   - Перевіряє розмір шрифту у перших 5 видимих елементах (параграфи, заголовки)
   - Перевіряє, чи розмір шрифту не менший за 12 пікселів (мінімум для читабельності)

**Навіщо це потрібно:** Якщо текст занадто малий, його важко читати. Особливо на мобільних пристроях.

**Простими словами:** На всіх пристроях текст має бути достатньо великим, щоб його можна було читати (не менше 12 пікселів).

---

### ✅ Перевірка 6: interactive elements (buttons, links) are clickable on all viewports

**Що перевіряє:** Чи клікабельні інтерактивні елементи (кнопки, посилання) на всіх розмірах екранів?

**Як працює:**
1. Перевіряє на трьох розмірах екранів: mobile, tablet, desktop
2. Для кожного розміру:
   - Знаходить кнопки та посилання (перші 3 видимі)
   - Перевіряє розмір кожної кнопки:
     - На mobile: мінімум 44×44 пікселів (стандарт для touch-таргетів - щоб легко було натиснути пальцем)
     - На desktop: мінімум 24×24 пікселів (стандарт для кліку мишкою)

**Навіщо це потрібно:** Якщо кнопка занадто мала, її важко натиснути, особливо на телефоні.

**Простими словами:** На телефоні кнопки мають бути достатньо великими (44×44 пікселі), щоб їх легко було натиснути пальцем. На комп'ютері можуть бути меншими (24×24 пікселі).

---

## 📋 Тест-кейс #15: Accessibility

**Що це означає?** Accessibility (доступність) - це коли сайт можуть використовувати всі люди, включаючи тих, хто:
- Використовує клавіатуру замість миші
- Використовує скрін-рідер (програма, яка читає текст голосом)
- Має проблеми зі зором або слухом

### ✅ Перевірка 1: keyboard navigation: Tab key moves focus between interactive elements

**Що перевіряє:** Чи переміщує клавіша Tab фокус між інтерактивними елементами (кнопками, посиланнями, полями)?

**Що таке фокус?** Фокус - це коли елемент "активний" і готовий до взаємодії. Зазвичай він виділяється рамкою або підсвічуванням.

**Як працює:**
1. Встановлює фокус на `<body>` (початок сторінки)
2. Натискає клавішу Tab
3. Перевіряє, чи фокус перемістився на інтерактивний елемент (кнопку, посилання, поле вводу)

**Навіщо це потрібно:** Деякі користувачі не можуть використовувати мишу (наприклад, через травму руки). Вони використовують клавіатуру для навігації.

**Простими словами:** Якщо натиснути Tab, фокус має переміститись на кнопку або посилання, а не залишитись на порожньому місці.

---

### ✅ Перевірка 2: keyboard navigation: Enter key activates buttons

**Що перевіряє:** Чи активує клавіша Enter кнопки?

**Як працює:**
1. Знаходить першу видиму кнопку на сторінці
2. Встановлює на неї фокус (якби натиснули Tab)
3. Перевіряє, чи кнопка видима і не заблокована (`disabled`)
4. (Натискання Enter не перевіряється напряму, але перевіряється, що кнопка готова до активації)

**Навіщо це потрібно:** Користувачі, які використовують клавіатуру, мають змогу активувати кнопки клавішею Enter.

**Простими словами:** Якщо натиснути Tab, щоб вибрати кнопку, а потім натиснути Enter, кнопка має спрацювати.

---

### ✅ Перевірка 3: keyboard navigation: Esc key closes modals (if modal is open)

**Що перевіряє:** Чи закриває клавіша Esc модалки (спливаючі вікна), якщо вони відкриті?

**Як працює:**
1. Знаходить CTA кнопку (якщо є) і клікає на неї, щоб відкрити модалку
2. Чекає 500 мілісекунд
3. Перевіряє, чи модалка відкрилась
4. Якщо модалка відкрита:
   - Натискає клавішу Esc
   - Чекає 500 мілісекунд
   - Перевіряє, чи модалка закрилась (не видима)

**Навіщо це потрібно:** Користувачі, які використовують клавіатуру, мають змогу закривати модалки клавішею Esc.

**Простими словами:** Якщо відкрити модалку (спливаюче вікно), натискання Esc має її закрити.

---

### ✅ Перевірка 4: focus is visible on interactive elements

**Що перевіряє:** Чи видимий фокус на інтерактивних елементах (коли на них натискають Tab)?

**Як працює:**
1. Знаходить перший інтерактивний елемент (кнопку, посилання, поле)
2. Встановлює на нього фокус
3. Перевіряє, чи елемент отримав фокус
4. Перевіряє, чи є видимі стилі фокусу (outline або box-shadow)

**Навіщо це потрібно:** Якщо фокус не видимий, користувач не знає, на якому елементі він зараз знаходиться.

**Простими словами:** Коли натискаєш Tab, елемент, на якому фокус, має бути виділений (рамкою або підсвічуванням), щоб було видно, де ти зараз знаходишся.

---

### ✅ Перевірка 5: ARIA attributes are properly used (aria-expanded for accordions/menus)

**Що перевіряє:** Чи правильно використовуються ARIA атрибути (особливо `aria-expanded` для акордеонів і меню)?

**Що таке ARIA?** ARIA (Accessible Rich Internet Applications) - це атрибути HTML, які допомагають скрін-рідерам зрозуміти, що робить елемент.

**Що таке `aria-expanded`?** Це атрибут, який показує, чи розгорнутий елемент (акордеон, меню). Може бути `true` (розгорнуто) або `false` (згорнуто).

**Як працює:**
1. Знаходить всі акордеони та меню (елементи з `aria-expanded`, або `<details>`)
2. Для кожного перевіряє:
   - Якщо є `aria-expanded`, він має бути `"true"` або `"false"` (не порожній)
   - Якщо це `<details>` і він розгорнутий, має бути атрибут `open`

**Навіщо це потрібно:** Скрін-рідери використовують ARIA атрибути, щоб зрозуміти стан елементів (наприклад, чи розгорнутий акордеон).

**Простими словами:** Якщо є акордеон (елемент, який розгортається/згортається), він має мати атрибут `aria-expanded="true"` (якщо розгорнутий) або `aria-expanded="false"` (якщо згорнутий).

---

### ✅ Перевірка 6: semantic HTML elements are used (nav, main, header, footer)

**Що перевіряє:** Чи використовуються семантичні HTML елементи (`<nav>`, `<main>`, `<header>`, `<footer>`)?

**Що таке семантичні елементи?** Це HTML теги, які описують, що робить елемент:
- `<nav>` - навігація (меню)
- `<main>` - основний контент
- `<header>` - шапка сторінки
- `<footer>` - підвал сторінки

**Як працює:**
1. Шукає семантичні елементи:
   - `<nav>` або `[role="navigation"]` (навігація)
   - `<main>` або `[role="main"]` (основний контент)
   - `<header>` або `[role="banner"]` (шапка)
   - `<footer>` або `[role="contentinfo"]` (підвал)
2. Для кожного знайденого елемента перевіряє, чи він видимий
3. Логує, скільки знайдено елементів кожного типу

**Навіщо це потрібно:** Скрін-рідери використовують семантичні елементи, щоб зрозуміти структуру сторінки.

**Простими словами:** Сторінка має мати структуру: `<header>` (шапка), `<nav>` (меню), `<main>` (основний контент), `<footer>` (підвал). Це допомагає скрін-рідерам зрозуміти, де що знаходиться.

---

### ✅ Перевірка 7: heading hierarchy is logical (H1 before H2, H2 before H3, etc.)

**Що перевіряє:** Чи логічна ієрархія заголовків (H1 перед H2, H2 перед H3, і т.д.)?

**Як працює:**
1. Знаходить всі заголовки (H1-H6) в основному контенті
2. Перевіряє порядок:
   - H1 має бути першим (або хоча б один H1 має бути)
   - H2 може йти після H1
   - H3 може йти після H2
   - Не можна йти назад (наприклад, H3 після H2, а потім знову H2 - це нелогічно)
3. Логує попередження, якщо ієрархія нелогічна

**Навіщо це потрібно:** Логічна ієрархія заголовків допомагає скрін-рідерам і користувачам зрозуміти структуру сторінки.

**Простими словами:** Заголовки мають йти в правильному порядку: спочатку H1 (головний заголовок), потім H2 (підзаголовки), потім H3 (під-підзаголовки). Не можна стрибати назад (H3 → H2).

---

### ✅ Перевірка 8: buttons are used for actions, links are used for navigation

**Що перевіряє:** Чи використовуються кнопки для дій, а посилання - для навігації?

**Як працює:**
1. Перевіряє, чи немає кнопок з атрибутом `href` (кнопки не мають бути посиланнями)
2. Перевіряє посилання:
   - Посилання мають мати `href` (не порожній)
   - Якщо `href="#"` (якір), логує попередження (краще використовувати кнопку для дій)

**Навіщо це потрібно:** Кнопки та посилання мають різні ролі:
- Кнопки - для дій (наприклад, "Відправити форму")
- Посилання - для навігації (наприклад, "Перейти на іншу сторінку")

**Простими словами:** Якщо хочеш зробити дію (наприклад, відкрити модалку), використовуй кнопку (`<button>`). Якщо хочеш перейти на іншу сторінку, використовуй посилання (`<a href="...">`).

---

### ✅ Перевірка 9: form inputs have associated labels or aria-label

**Що перевіряє:** Чи мають поля форми асоційовані labels (мітки) або `aria-label`?

**Що таке label?** Label - це текст, який пояснює, що треба ввести в поле (наприклад, "Ім'я", "Email").

**Як працює:**
1. Знаходить всі поля форми (`<input>`, `<textarea>`, `<select>`), які видимі
2. Для кожного поля перевіряє:
   - Чи є `id` і чи є `<label for="id">` (асоційована мітка)
   - Чи є `aria-label` (альтернативна мітка)
   - Чи є `aria-labelledby` (посилання на інший елемент з текстом)
   - Чи є `placeholder` (підказка в полі)
3. Для обов'язкових полів (`required`) мітка обов'язкова
4. Для необов'язкових полів мітка рекомендована (логуються попередження, якщо немає)

**Навіщо це потрібно:** Скрін-рідери використовують labels, щоб зрозуміти, що треба ввести в поле. Без labels користувач не знає, що вводити.

**Простими словами:** Кожне поле форми має мати мітку (label), яка пояснює, що треба ввести. Наприклад, біля поля "Email" має бути текст "Email" або "Введіть ваш email".

---

## 📋 Тест-кейс #16: Discount section

**Що це означає?** Discount section - це секція зі знижками та цінами на сторінці курсу. Вона показує вартість курсу, можливі знижки, переваги та кнопку для запису.

**Навіщо це потрібно:** Це важлива секція, яка впливає на рішення користувача записатися на курс. Потрібно переконатися, що вона працює правильно на всіх пристроях.

---

### ✅ Перевірка 1: discount section exists and is visible

**Що перевіряє:** Чи існує секція discount на сторінці і чи вона видима?

**Як працює:**
1. Шукає секцію discount за селектором `section.discount.section` (з конфігурації `pages.json`)
2. Перевіряє, чи елемент існує в DOM
3. Перевіряє, чи елемент видимий (не прихований через `display: none` або `visibility: hidden`)
4. Перевіряє, чи знайдено хоча б один елемент

**Навіщо це потрібно:** Якщо секції немає або вона прихована, користувач не побачить ціну та не зможе записатися на курс.

**Простими словами:** Секція з ціною має бути на сторінці і має бути видимою для користувача.

---

### ✅ Перевірка 2: discount section has a heading or title

**Що перевіряє:** Чи має секція discount заголовок або назву?

**Як працює:**
1. Шукає заголовки (H2-H6) всередині секції
2. Шукає елементи з класами, що містять "title" або "heading"
3. Перевіряє, чи є в секції осмислений текст (більше 10 символів)
4. Якщо хоча б одна з перевірок пройшла - секція має заголовок

**Навіщо це потрібно:** Заголовок допомагає користувачу зрозуміти, що це секція з ціною/знижками.

**Простими словами:** Секція має мати заголовок, наприклад "Вартість курсу" або "Ціна".

---

### ✅ Перевірка 3: discount section contains price information

**Що перевіряє:** Чи містить секція discount інформацію про ціну?

**Як працює:**
1. Отримує весь текст з секції
2. Шукає символи валюти (₴, $, €, £, ₽) з числами
3. Шукає слова, пов'язані з ціною (цін, price, стоимость, вартість, грн)
4. Шукає елементи з класами, що містять "price", "cost", "amount"
5. Шукає елементи з data-атрибутами `data-price`, `data-cost`
6. Якщо хоча б одна з перевірок знайшла щось - секція містить інформацію про ціну

**Навіщо це потрібно:** Користувач має бачити ціну курсу, щоб прийняти рішення про запис.

**Простими словами:** В секції має бути видно ціну, наприклад "30 000 грн" або "3 000 грн/міс".

---

### ✅ Перевірка 4: discount section has list of benefits or features (if present)

**Що перевіряє:** Чи має секція discount список переваг або особливостей (якщо він є)?

**Як працює:**
1. Шукає списки (`<ul>`, `<ol>`) всередині секції
2. Шукає зображення з alt-текстом, що містить "check" (галочки)
3. Шукає елементи з класами, що містять "benefit" або "feature"
4. Якщо хоча б одна з перевірок знайшла щось - логує, що список переваг є
5. Якщо нічого не знайдено - логує, що список опціональний (не обов'язковий)

**Навіщо це потрібно:** Список переваг допомагає користувачу зрозуміти, що входить у курс.

**Простими словами:** В секції може бути список переваг (наприклад, "Повний доступ до уроків", "Практичні завдання"), але це не обов'язково.

---

### ✅ Перевірка 5: discount section has CTA button (opens modal)

**Що перевіряє:** Чи має секція discount кнопку CTA (Call To Action), яка відкриває модалку?

**Як працює:**
1. Шукає кнопку з атрибутом `data-modal-open` всередині секції
2. Якщо кнопка знайдена:
   - Перевіряє, чи вона видима
   - Клікає на неї
   - Перевіряє, чи відкрилася модалка
   - Закриває модалку
3. Якщо кнопка не знайдена - логує, що це опціонально (не обов'язково)

**Навіщо це потрібно:** Кнопка дозволяє користувачу записатися на курс прямо з секції з ціною.

**Простими словами:** В секції має бути кнопка "Записатися на курс", яка відкриває форму для запису. Примітка: Сама форма тестується в тест-кейсі #5, тут ми тільки перевіряємо, що кнопка відкриває модалку.

---

### ✅ Перевірка 6: discount section works on mobile viewport

**Що перевіряє:** Чи працює секція discount на мобільному пристрої?

**Як працює:**
1. Встановлює мобільний viewport (375x667 - розмір iPhone)
2. Чекає 1 секунду, щоб сторінка адаптувалася до мобільного розміру
3. Перевіряє, чи секція існує і видима
4. Перевіряє, чи секція має ширину та висоту (не нульові розміри)
5. Перевіряє, чи інформація про ціну все ще видима на мобільному

**Навіщо це потрібно:** Багато користувачів переглядають сайт на мобільних пристроях. Секція має бути читабельною і доступною на маленьких екранах.

**Простими словами:** Секція з ціною має добре виглядати і працювати на телефоні, не тільки на комп'ютері.

---

## 🎓 Підсумок

**Всього перевірок:**
- **Тест-кейс #1 (Smoke/Sanity/SEO):** 16 перевірок
- **Тест-кейс #2 (Header/Menu/Navigation):** 6 перевірок
- **Тест-кейс #3 (Language switcher):** 6 перевірок
- **Тест-кейс #4 (Hero block):** 4 перевірки
- **Тест-кейс #5 (Modal form):** 8 перевірок
- **Тест-кейс #6 (Benefits section):** 6 перевірок
- **Тест-кейс #7 (About section):** 5 перевірок
- **Тест-кейс #8 (Course program):** 6 перевірок
- **Тест-кейс #9 (Learning format):** 5 перевірок
- **Тест-кейс #10 (Reviews):** 5 перевірок
- **Тест-кейс #11 (Pricing section):** 5 перевірок
- **Тест-кейс #12 (Footer):** 10 перевірок
- **Тест-кейс #13 (Analytics / dataLayer):** 6 перевірок
- **Тест-кейс #14 (Responsive / Cross-browser):** 6 перевірок
- **Тест-кейс #15 (Accessibility):** 9 перевірок
- **Тест-кейс #16 (Discount section):** 6 перевірок

**Всього: 98 перевірок** (для кожної сторінки з `pages.json`)

---

## 📄 Сторінки для тестування

Всі тести автоматично виконуються для кожної сторінки, визначеної в `cypress/fixtures/pages.json`:

### 🇺🇦 Українська локаль (ua):
1. **AI Automator** (`ai-marketing-specialist`)
   - URL: `https://goit.global/ua/courses/ai-marketing-specialist/`
   - H1: "AI Automator"
   - Zoho Product ID: `1819773001945816767`

2. **AI Content Maker** (`ai-content-maker`)
   - URL: `https://goit.global/ua/courses/ai-content-maker/`
   - H1: "AI Content Maker"
   - Zoho Product ID: `1819773001939221031`

3. **Prompt Engineering** (`prompt-engineer`)
   - URL: `https://goit.global/ua/courses/prompt-engineer/`
   - H1: "Prompt Engineering"
   - Zoho Product ID: `1819773001936699021`

4. **No-Coder + AI** (`ai-no-coder`)
   - URL: `https://goit.global/ua/courses/ai-no-coder/`
   - H1: "No-Coder + AI"
   - Zoho Product ID: `1819773001945816753`

### 🇷🇺 Російська локаль (ua-ru):
1. **AI Automator (RU)** (`ai-marketing-specialist-ru`)
   - URL: `https://goit.global/ua-ru/courses/ai-marketing-specialist/`
   - H1: "AI Automator"
   - Zoho Product ID: `1819773001945816767`

2. **AI Content Maker (RU)** (`ai-content-maker-ru`)
   - URL: `https://goit.global/ua-ru/courses/ai-content-maker/`
   - H1: "AI Content Maker"
   - Zoho Product ID: `1819773001939221031`

3. **Prompt Engineering (RU)** (`prompt-engineer-ru`)
   - URL: `https://goit.global/ua-ru/courses/prompt-engineer/`
   - H1: "Prompt Engineering"
   - Zoho Product ID: `1819773001936699021`

4. **No-Coder + AI (RU)** (`ai-no-coder-ru`)
   - URL: `https://goit.global/ua-ru/courses/ai-no-coder/`
   - H1: "No-Coder + AI"
   - Zoho Product ID: `1819773001945816753`

**Всього сторінок: 8** (4 курси × 2 мови)

**Якщо в `pages.json` 8 сторінок (4 курси × 2 мови), то:**
- 98 перевірок × 8 сторінок = **784 тести** загалом!

---

## 💡 Поради для новачків

1. **Не лякайся коду** - він просто автоматизує те, що ти робиш вручну
2. **Кожна перевірка має сенс** - вона перевіряє щось важливе для користувача
3. **Якщо тест падає** - це не завжди погано, це означає, що знайдена проблема
4. **Тести допомагають** - вони знаходять помилки швидше, ніж ручна перевірка

---

## 📖 Словник термінів

- **Test (тест)** - автоматична перевірка
- **Assertion (перевірка)** - перевірка, що щось правильне
- **Element (елемент)** - частина HTML (кнопка, поле, текст)
- **Selector (селектор)** - спосіб знайти елемент (наприклад, `#name-input`)
- **Viewport (вікно перегляду)** - розмір екрана браузера
- **Locale (локаль)** - мова/регіон (ua, ua-ru)
- **Modal (модалка)** - спливаюче вікно
- **Form (форма)** - форма з полями для заповнення
- **Validation (валідація)** - перевірка правильності даних
- **Stub (заглушка)** - фейковий відгук від сервера (для тестування без реальних даних)

---

**Якщо щось незрозуміло - питай!** 😊

